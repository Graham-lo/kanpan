# 图表出厂指标

现行 `IndicatorID.defaultSubs` 为成交量、持仓量、平滑异同均线（`.vol, .oi, .macd`），副图同时最多三个。主图均线出厂周期10 / 30 / 120 / 256。

默认值迁移只覆盖仍保留旧出厂组合的档案，不覆盖用户手动选择，不通过清空存档改变显示。与其它项目的默认指标约定互不套用。实现与验收以当前源码和 `.project-memory/PROJECT.md` 为准。
