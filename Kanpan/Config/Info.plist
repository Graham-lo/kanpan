<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  这份 Info.plist 只放 Xcode 自己生成不了的那几个键，其余键仍由
  `GENERATE_INFOPLIST_FILE = YES` 生成后合并进来（两者可以同时用）。

  为什么不能继续写 `INFOPLIST_KEY_KanpanAccountAPIURL`：`INFOPLIST_KEY_*` 这套
  构建设置只认 Xcode 内置的那张已知键表（CFBundleDisplayName、UILaunchScreen 之类），
  自定义键会被**静默丢弃**——不报错、不警告，打出来的包里就是没有。结果是
  `Bundle.main.object(forInfoDictionaryKey: "KanpanAccountAPIURL")` 取到 nil，
  `AccountFeature` 建不出 `AccountClient`，登录/注册一律回「账号服务暂不可用」，
  看着像后端挂了，其实后端一直好好的。
-->
<plist version="1.0">
<dict>
  <!--
    启动屏的底色。必须手写在这儿，不能交给 `INFOPLIST_KEY_UILaunchScreen_*`。

    2026-09-19 在 Xcode 26（17F113）上逐条对照构建量过：
    · `INFOPLIST_KEY_UILaunchScreen_Generation = YES` 生成出来的是
      `UILaunchScreen = { UILaunchScreen = {} }`——多套了一层同名空字典；
    · `INFOPLIST_KEY_UILaunchScreen_BackgroundColor = LaunchBackground` 单独留着时，
      包里连 `UILaunchScreen` 这个键都不出现，它是彻底的空转；
    · 两条一起写，得到的就是那个**空字典**，`LaunchBackground` 一次都没被用上。
    没有 `UIColorName` 的启动屏，系统按纯白铺——于是选了深色的人每次冷启动先吃一屏白，
    再被系统的启动屏淡出动画拖成一条白→深的渐变。

    这份 `Info.plist` 和 `GENERATE_INFOPLIST_FILE` 生成的那份是**按键合并**的
    （对照构建里我们手写的 `UIColorName` 和生成的那个空字典同时出现在包里），
    所以手写这一条不会被生成器盖掉；pbxproj 里那两条已经删掉，免得再合进来一坨空字典。

    颜色只能跟**系统**深浅走（`Assets.xcassets/LaunchBackground.colorset` 的
    Any/Dark 两档）：启动屏是 app 进程起来之前由系统渲染并缓存的，读不到 UserDefaults，
    更读不到账号档案里的皮肤与深浅。app 自己那一帧跟着人走，见 `LaunchThemeMirror`。
  -->
  <key>UILaunchScreen</key>
  <dict>
    <key>UIColorName</key>
    <string>LaunchBackground</string>
  </dict>
  <key>KanpanAccountAPIURL</key>
  <string>https://kanpan.107-174-172-10.sslip.io</string>
  <!--
    不加这一条，ProMotion 的 iPhone 上 CADisplayLink 会被系统钳死在 60 Hz：
    `preferredFrameRateRange(80…120)` 照样能设，但一帧都不会多给。
    图表拖 / 捏 / 甩全靠 DisplayLink 推帧，钳在 60 Hz 就是「跟手一半」。
    iPad 不受此限制，这个键只对 iPhone 生效；副作用是静止时也允许高刷，
    所以 `ChartView.resumeLink()` 必须按脏位来源把非手势帧压回 60。
  -->
  <key>CADisableMinimumFrameDurationOnPhone</key>
  <true/>
  <!--
    出口合规。不写这一条，每个传上 App Store Connect 的构建都会停在
    「缺少出口合规信息」那一步，要人进网页里一问一答填一遍才能发给测试者——
    一个构建填一次，TestFlight 一天传几版就要填几版。

    填 false 的依据（2026-09-19 全仓库核过一遍）：app 自己不做任何加解密，
    只用系统给的 HTTPS / WSS（`URLSession` / `URLSessionWebSocketTask`）与
    Keychain（`KanpanAccount/Sources/KanpanAccount/CredentialVault.swift`、
    `KanpanReview/Sources/ReviewData/ReviewCredentials.swift`，都只是
    `SecItemAdd` / `SecItemCopyMatching` 存取会话令牌）。这两样都在
    Apple 的豁免清单里。仓库里唯一 `import CryptoKit` 的地方是
    `KanpanChart/Tests/KanpanChartTests/EvidenceRenderTests.swift`，
    拿 SHA256 给取证图算指纹，在测试 target 里，不随 App Store 包发出去。

    以后如果真加了自研加密（自己实现的对称/非对称算法、端到端加密的同步等），
    这里不能继续写 false：要么改成 true 并走年度自查报告，要么按新情况重判。
  -->
  <key>ITSAppUsesNonExemptEncryption</key>
  <false/>
</dict>
</plist>
