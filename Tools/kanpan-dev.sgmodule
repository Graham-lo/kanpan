#!name=看盘 开发期：只放行本项目进程
#!desc=只让 kanpan-feed / Kanpan 这两个本项目进程访问币安行情流时走代理组，其余进程（含手机、币安 App、浏览器）一律维持主配置的 DIRECT 优化，一个字不改。
#!system=mac

[Rule]
# 背景：
#
# 主配置第 256 行挂的 BinanceDirect.list 把 fstream/stream/dstream/nbstream
# 钉死成 DIRECT，理由写在它自己的注释里——在网关上抓币安 App，行情流是从国内
# 加速 IP（39.x / 47.x / 106.x 阿里云腾讯云广东段）来的，直连 28ms、走代理 172ms。
# 那是给手机上的币安 App 调的，是对的，不动它。
#
# 但 Mac 上的 [Host] 段（主配置 340-343 行）把同一批域名交给 DoH 解析，解出来是
# 35.73.130.195 这种 AWS 东京地址，不在国内加速段里。于是「内地线路裸连境外 IP」，
# TLS Client Hello 发出去就被 RST。这个模块不去掰那个矛盾，只是给本项目开个小口。
#
# 做法：AND 逻辑规则，进程名 + 域名两个条件同时成立才改走向。命中面就是
# 「Mac 上这两个二进制访问 binance.com」，别的什么都不碰。
# 模块的 [Rule] 插在主配置规则之前，所以能抢在 BinanceDirect.list 前面命中。

# 取证命令行：KanpanData/.build/release/kanpan-feed
AND,((PROCESS-NAME,kanpan-feed),(DOMAIN-SUFFIX,binance.com)),Binance
# 模拟器里的 app（模拟器 app 在 macOS 上就是同名宿主进程）
AND,((PROCESS-NAME,Kanpan),(DOMAIN-SUFFIX,binance.com)),Binance
