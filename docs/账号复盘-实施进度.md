# 账号、复盘与行情接入状态

> **部分历史（2026-09-18 标注）**：「已实现并部署」「行情体验修复」两节里凡是讲行情线路的——Binance / OKX 整套自动切换、成功线路保存在 Application Support、后台恢复检测与切换提示文案、底栏入口「复盘」、风格并入图表——都已被 2026-09-17 的「行情线路两档、用户自己选、没有自动切换」和 2026-09-18 的常驻标签栏取代，对应代码（`market-source.json`、`MarketRecoverySchedule`、直连冷却 / 对冲）已删除。现行界面与线路见根目录 `README.md`。本文现在仍然当数的是最后一节「尚未完成的复盘范围」。

2026-09-15。本文覆盖本轮真实完成情况，替代此前邮件认证和未部署阶段的状态。

## 已实现并部署

- 账号改为用户名 + 密码直接注册/登录，暂停邮箱工作；设备管理、改密、注销、Keychain 会话和轮换保留。
- 独立 `kanpan-api` / worker 已部署到主 VPS；独立 PostgreSQL、非特权运行角色、强制 RLS 隔离个人数据，端上按账号目录切换。
- API 监听 8794，数据库只绑定本机 55434；未占用原图像服务端口，未重启旧 Scorebook。每日数据库备份保留 30 天（同机备份）。
- 指标、颜色、画线、自选和复盘使用持久待发队列与字段同步；个人记录按页读取，行情按视野补页，不全量下载历史 K 线。
- 独立 `KanpanReview` 模块接现有图表：记一笔、复盘列表/待办/统计、详情、逐根重温、私有 OHLC 找相似；复盘记录固定所属行情源。
- 图表整套 Binance / OKX 自动切换，历史、补缺与 WS 不混源；两台 VPS 已部署 OKX REST 和共享 WS 网关。

## 行情体验修复

- 切品种/周期取消旧请求产生的 `URLError.cancelled (-999)` 不再误判为断网，不再让其他品种一起进入失败冷却。
- 单次普通超时不再全局禁用币安直连；正常网络直接加载，无启动 REST/WS 额外验通关卡。
- 成功线路保存在 Application Support；重启、切品种、切周期直接沿用。
- 正常 OKX 使用期间，后台恢复检测首次等待 5 分钟；连续失败逐步延长到 15 分钟。页面切换不重置检测期限。真实网络变更可提前一次，60 秒内抖动不重复触发。
- 恢复须连续两次通过历史 REST 和真实 WS 帧验证，后台无弹窗。切换期间保留图表，显示“检测到当前链路不可用，正在切换智能链路”；实际历史与 WS 均就绪后提示“已切换成功”。两套均不可用才显示简短重试提示。
- 正常图表不再被复盘选择层拦截拖动。风格并入图表，底部入口为“复盘”。

## 测试证据

- KanpanCore：209 项；KanpanData：90 项；KanpanAccount 存储/隔离：4 项；ReviewStorage：4 项（iOS 26.5 模拟器）；网关：26 项；后端：7 项单元 + 1 项真实 PostgreSQL 集成通过。
- 真实 OKX 300 根首屏、1500 根历史补页、实时推送、线路落盘与重启恢复通过（`/tmp/kanpan-live-router-final.log`）。
- iPhone 16 Pro / iOS 26.6.1：10 种画线和手指端点命中、均线颜色取消/保存/重启、精简风格与自动旋转分别通过。此前完整画线交互见 `acceptance/drawing-v2/DEVICE.md`。
- 用户实际网络日志验证币安直连 HTTP 200 / 198ms；已定位并修复取消请求导致其他品种误切换。修复后的真实手机 XRP/SOL 切品种与周期加载约 0.9–1.3 秒，并有缓存命中。
- Release 构建通过并覆盖安装到该手机；16:30 启动现有持久线路，300 根历史返回约 1.7 秒。未修改用户的 Mac 网关/代理配置。
- iOS 26.5 模拟器端到端两项通过：正常 Binance 加载；OKX 历史补页、用户名注册、重启保持登录、注销（`/tmp/kanpan-final-account-simulator-r4.log`）。测试显式处理系统强密码弹层，断言包含实际账号身份。
- 最新行情提示与所有新增复盘交互尚未完成整套真机回归，不宣称全部真机验收完成。

## 尚未完成的复盘范围

- 框选贴边自动滚动、精确时间编辑与痕迹点进详情的完整交互。
- 保存案例的管理 UI、规则版本及完整修订查看、截图补录与旧资料归属导入。
- 公开历史相似索引已完成首批导入：主节点 5354 个已发布窗口，Binance 2677 + OKX 2677；两套均为 USDⓈ-M、10 个主流 USDT 永续、最近 180 天、1h，使用 `candle-geometry-v2`。这是可用种子，不代表全市场、全周期覆盖；查询按行情源隔离。
- OKX 精确 trade_touch 证据不足时保留待核验；不拿 Binance 结果或普通 OHLC 冒充。OKX 持仓量副图尚未接入。
- 跨设备并发、同步冲突及全部复盘手机操作的最终端到端回归。其他旧系统/机型兼容性按用户要求后续单独处理。
