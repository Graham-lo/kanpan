# 看盘 · 未完成事项盘点（2026-09-22）

这份清单**逐条在当前工作树上核对过**（`grep` 计数 + 读代码），不是从上一轮的清单抄下来的。
之所以要重新核，是因为上一版清单里已经发现两条和现实对不上（见「四」）——照着过期的清单
干活会做错东西。

核对基准：`26f8d68`。

---

## 一、零实现，但**不缺基建**（最划算，应该先做）

接口都在已经接通的那一族里，属于增量不是开荒。

| 项 | 现状 | 为什么便宜 |
| --- | --- | --- |
| 多空比 | 0 | `/futures/data/*` 下，和已接好的 `openInterestHist` 同一族，主机 / 限流 / 解析套路现成 |
| 主动买卖比 | 0 | 同上 |
| 基差 | 0 | 同上 |
| 强平 forceOrder | 0 | 新流，但组合流机制现成 |
| aggTrade 逐笔明细 | 0 | 新流，同上 |
| 五档深度 | 部分 | `@bookTicker` 已订但只有买一卖一，差 `@depth5`；UI 侧 8 处命中需细看 |

已订阅的流：`@ticker`、`@markPrice`、`@kline_`、`@bookTicker`。
已实现的 REST：`exchangeInfo`、`klines`、`ticker`、`premiumIndex`、`openInterestHist`。

## 二、零实现，要从头做

**行情 / 图表**
- 资金费计算器
- 枢轴点
- 对比 K 线
- 法币（CNY）换算
- 自定义周期 —— `KanpanCore/.../Interval.swift:4` 是写死的 14 档枚举
- 指标菜单分类
- 点图例隐藏（X14）

**账号**
- 2FA、昵称、头像
- 忘记密码
- 数据导出
- `/me/reopen` 调用（服务端有没有另说，客户端零调用）
- 回收站恢复 undelete

**其它**
- 本地化 —— `NSLocalizedString` / `String(localized:)` **一处都没有**
- 关于 / 隐私页
- 提醒铃声选择
- 自选空态 Top 20
- 分享「回给他」回复 UI
- OKX 持仓量副图
- 多交易所抽象 —— `Coinbase` 仅 3 处命中，基本等于没有

**平台能力（见「五」，被账号卡住）**
- 小组件 Widget —— `WidgetKit` 零引用
- Handoff —— `NSUserActivity` 零引用
- 实时活动**客户端** —— `ActivityKit` 零引用，Info.plist 无 `NSSupportsLiveActivities`，
  工程只有 app + UI 测试两个 target

## 三、已有但没做完

| 项 | 现状 |
| --- | --- |
| `FrameProbe` | 整套写好了，**生产代码零调用点** |
| 相似度指数 | 42 处，seed-scale 问题待查 |
| 固定起点强弱比较 | 4 处，未成形 |
| `SymbolTickerFeed` | 5 处，A5.7 待查 |
| Y 轴连续曲线 | 0 —— 字体 / 颜色 / 音高测量一并没做 |
| iPad 面板宽度上限断言 | 待查 |
| 深色蜡烛色 | 从未在真机上校准过 |
| 复盘那七项 | 待查 |

## 四、上一版清单里**已经不成立**的（照着做会做错）

- **「币安权重上限写死 2400」—— 假的。** `KanpanNetwork/.../RateLimiter.swift:200-206`
  早就改成按交易所回的 `X-MBX-USED-WEIGHT-1M` 头动态折算（87.5%），2400 只剩注释里那个比例。
- **「Dynamic Type 零覆盖」—— 不准确。** 是 9 处，但全挤在 `SymbolSearchView` 和
  `SymbolPickerView` 两个文件里。自选、图表头部、设置、账号、提醒、板块一概固定字号。
  缺口是真的，形状不一样。

## 五、被「Xcode 没登 Apple ID」卡住的

Xcode 里一个 Apple ID 都没有（`error: No Accounts`，`IDEAccountStore.plist` 不存在）。后果：

1. 手机上那个包的描述文件 **2026-09-22T08:48:26Z（16:48 CST）到期**，到点起不来，我重签不了。
2. 小组件、实时活动客户端都要新 target → 要新描述文件 → 做不了真机验证。
3. **App Group 那个问题至今没有答案**——是卡在没账号，不是证明了做不到。

模拟器不需要真签名，所以这几项**可以先在模拟器上做完、验完**，账号一登就能上真机。
登录需要用户的密码和 2FA。

另：**服务端那一半已经部署上线并验过**（`/v1/devices/live-activity/end` GET 回 405、
无凭证 POST 回 401），但客户端整个不存在，**所以它现在没有任何东西可推**。

## 六、证据类欠账

- `docs/testflight-uploads.md` 表头在、**表体零记录**
- M0–M9 §12.3 签收没做
- A2.14 长稳从未跑过
- 冷启动从未逐帧测过
- `Tools/ui-test.sh` 缺 `transportType: wired` 前置检查
- 真机端到端提醒（画线 → 加提醒 → 价格触到 → 手机真的震 + 浮条）从未走通

---

## 附：这一轮已经做掉的

- 左滑删除收成一个组件，系统红退场，六套皮肤 5.62～8.77:1（`d753bf8`）
- 矩阵脚本三处并行隔离：`DD` / `RES` / `SUFFIX`（`c269f82`、`3b86e35`）
- `Tools/make-sim-set.sh`，那套专属模拟器变成可重建的（`26f8d68`）
