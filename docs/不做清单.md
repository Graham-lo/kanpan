# 看盘 · 不做清单（唯一口径）

**这份清单是看盘「不做什么」的唯一口径。** 仓库里任何其它文档——审查报告、盘点、方案、实施提示词、
交接书、`README.md`、`.project-memory/PROJECT.md`、代码注释——把下面这些功能写成「待办 / 建议 / 缓 /
预埋 / 可选 / 以后做」的，一律无效，不得据此实现。要翻案只有用户本人开口，并且同一次改动里把本清单改掉。

**新判「不做」的项**：当场写进本清单，并把它在其它文档里的「待办 / 建议」措辞删掉或改成
「不做（见 docs/不做清单.md）」。不许只在原文档里加一个「⏸ 缓」就算完。

**任何窗口（Claude、Codex、子代理）开工前先读本清单**，再读 `docs/待办交接-Codex-2026-09-22.md`
（当前唯一的待办来源）。`docs/` 下其它文档是规格或历史记录，不是待办来源。

| 项 | 谁定的 / 为什么 |
| --- | --- |
| 强平 forceOrder（副图、列表、后端采集，全部） | 用户 2026-09-22 定的。币安只有实时流没有历史，做完整要自家后端常驻攒，不是增量 |
| 法币（CNY）换算 | `docs/AICoin-盘点原始清单-2026-09-17/01-品种详情页.md:350`「建议看盘不做」，用户 2026-09-22 确认不做 |
| 固定起点强弱比较（选品种 + 选起点 + 归零叠线） | `docs/提醒与体验细节-实施方案-2026-09-20.md:214` 判「⏸ 缓」，用户确认不做 |
| 板块二阶变化 / 加速度 | 实施方案里已砍，研究文档 `板块强弱与二阶变化-算法研究-2026-09-18.md` §12 需回标（见 P4） |
| 对比 K 线 | 和「固定起点强弱比较」是同一家的东西，一并不做 |
| 点图例隐藏（X14） | 撞「画布上不浮控件」；等价入口已在指标面板逐输出开关（`Panels/IndicatorPanel.swift:284-285` → `Prefs.hiddenOutputs`） |
| 市场页 / 全市场排行、分析 Tab、涨跌分布、全市场费率排行、费率历史、秒级持仓量 | 底栏五格钉死（`Main/TabBar.swift:20`），没有第六页；板块页就是全市场视图。本次拍板不做 |
| 自建分组 + 自选批量编辑 | 自选分类页是用户自己定稿的（固定枚举 `FavoriteCategory`），不动 |
| 资金费计算器 | 头部已有费率 + 倒计时，没有承载位置 |
| 自定义周期（任意周期聚合） | `Interval.swift` 14 档钉死，本次不做 |
| 2FA、昵称、头像、忘记密码（客户端）、提醒铃声选择、本地化（多语言）、多交易所抽象 / Coinbase、OKX 持仓量副图 | 账号是用户名 + 密码、3～10 人规模，产品只有中文；多交易所方案 `docs/多交易所行情接入方案-Coinbase现货-2026-09-17.md` 标着「待用户确认」，没拍板前不动 |
| `/me/reopen` 冷静期撤销、回收站 | 注销是硬删（`auth.rs:286-294`），维持；「恢复上一次删除」已由带撤销的 toast 覆盖 |
| 复盘「旧资料归属导入」 | Scorebook 网页版已不是主线（父目录 `CLAUDE.md`），不导 |
| 网关线路自动切换、任何 `*.binancefuture.com` 域名 | 既定规则 |

以上出处里引用的文档在 2026-09-22 的文档统一口径后可能已被删除或改写；引用只为说明当时是谁、在哪里定的。
