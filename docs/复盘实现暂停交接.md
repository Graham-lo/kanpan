# 复盘实现暂停点 · 2026-09-15

用户明确要求暂停，等待 Claude 设计后端，再统一审查、落地。禁止把当前草稿当成冻结契约或已完成能力。

2026-09-15 后续：已审查桌面最新账号与同步原型/要求，并结合用户的多人隔离、登录后云端为主、手机体验和精简文案要求，新增 `账号同步与复盘-最终落地方案.md`。最终设计以该文件为准；本轮只落实现有界面文案精简并完成编译检查，尚未继续账号与复盘后端实现或部署。

## 已保留

- 画线 v2 与 MA/EMA 颜色：真实 iPhone 16 Pro 验收结果见 acceptance/drawing-v2/DEVICE.md。
- 入口修改：底栏「复盘」；风格只在图表里，四种精选；手机自动转屏，没有单独横屏按钮。用户最新选择覆盖此前周期行/图角按钮方案。
- 独立 KanpanReview Swift Package：ReviewDomain、ReviewData、ReviewUI；草稿/记录/操作队列原子存储、Keychain 连接凭证、记录卡/复盘簿/搜索/回放控制草稿。
- App ReviewIntegration：冻结框选、触摸把手、复盘痕迹、真实行情游标回放，复用 ChartView；MainScreen 只负责入口和适配。
- 原生编译成功：/tmp/kanpan-review-build3.log。没有新增功能真机通过结论。
- scorebook-backend 本地有 native_review DTO/domain/application/routes 及 0057 迁移草稿；未部署、未执行迁移、未提交。后端 cargo check 在暂停前启动，随后自然完成（22.89 秒）；仅编译检查通过，未做功能测试。

## 必须等设计后审查的内容

- 草稿 API 为 /v1/native-review/*，不是最终契约。需审查是否扩展原 calls/criteria/review-workflow，而不是另建正式结果分支。
- 当前后端原生结果/复盘事件独立；旧记录列表/原复盘版本冲突/截图补录还没有接到原生界面。
- 当前统计用了简化的 5 天分组占位，不能作为正式统计落地；应复用冻结样本资格及代表规则。
- 记录创建的真实市场、范围、参考价服务端校验需补齐；当前 range 特征验证在异步作业里，不能未经验证就进正式战绩。
- 触价任务的逐笔边界与跨段连续性、收盘任务的缺口/到期/租约 fencing、结果修订都需逐项验证；当前代码是未测草稿。
- OpenAPI、响应契约、schema 注册、备份/导出/删除/GC 对新表的支持还未接入。
- 原生端离线多次复盘编辑的 expectedRevision 队列、账户切换时正在运行的同步任务、冲突保留/处理尚需完善。
- 框选端点有 44pt 命中范围，但贴边自动滚动/补历史、时间精确输入、恢复不同品种草稿尚需补全。
- 重温指标快照和价格坐标、图表 viewport、横屏入口仲裁/禁用、录入到回放的状态切换需完整检查；搜索 cutoff 必须冻结，不用重新渲染时的 now。
- 原有 UI 用例仍有 bottom.style/land.exit 旧入口引用，后续统一更新后再整套验收。
- 自动旋转沿用现有 iOS orientation 支持；当前用户不要手动横屏。无需改变系统旋转锁定设置。

## 保留其他修改

用户原有应用名称/签名/team/icon/brand 改动不可覆盖；本轮仅在 pbxproj 追加 KanpanReview 依赖。未改 scorebook-frontend，不重启 Claude 的前端。
