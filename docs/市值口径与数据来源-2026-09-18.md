# 行情页四格：持仓量 / 成交额 / 市值 / 费率 —— 市值口径与数据来源

2026-09-18。行情页头部只放这四格（用户点名，其他一律不加）。数额一律用
K / M / B / T 金融单位，不用万 / 亿。**市值 = 总市值 = 总供应量 × 现价**，
不是流通市值。

四格的来源：

| 格子 | 来源 |
| --- | --- |
| 持仓量 | VPS 的 `kanpan-api` `/v1/market/open-interest`，Binance / OKX 两个上游 |
| 成交额 | 客户端已订阅的 24h ticker 流，本来就有 |
| 市值 | VPS 的 `kanpan-api` `/v1/market/meta`，`totalSupply × 现价` |
| 费率 | 客户端已订阅的 markPrice 流（标签写「费率」，不写 FR） |

下面只讲市值。

## 1. 币安永续里有三分之一不是币

`https://www.binance.com/fapi/v1/exchangeInfo` 每行有个 `underlyingType`。
2026-09-18 的 766 个 TRADING 合约：

| underlyingType | 个数 | 是什么 |
| --- | --- | --- |
| COIN | 573 | 加密货币 |
| EQUITY | 156 | 美股正股，以及在美国上市的 ETF / 杠杆 ETP |
| HK_EQUITY | 15 | 港股 |
| KR_EQUITY | 8 | 韩股 |
| COMMODITY | 8 | 金银铂钯、原油、布油、天然气、铜 |
| INDEX | 2 | `BTCDOM`、`ALL` |
| CN_EQUITY | 2 | A 股（CXMT、UNITREE） |
| PREMARKET | 2 | 未上市公司（ANTHROPIC、OPENAI） |

按「币」的口径去算这 193 个非币合约，出来的一定是错数。所以市值先按
`underlyingType` 分流，认不出的**留空**，宁可空着也不给错的数。

## 2. 服务端存的不是股数，是乘数

服务端把 `k = 该公司真实市值(美元) ÷ 该合约当前价` 写进 `totalSupply`，
客户端照旧显示 `totalSupply × 现价`。

这样做的原因是：同一家公司在币安可能有几个报价口径完全不同的合约。

- `HK0700USDT` 按**港元**报价（426.00）
- `TENCENTUSDT` 按**美元每股**报价（54.90）
- `SKHYUSDT` 是 **ADR**，一份存托凭证只代表约 0.14 股

存股数的话，第三种必须知道折股比例才算得对。上一版拿 Nasdaq 筛选器的股数，
它把 SK 海力士的 ADS 比例当成整十，算出 1.34T，比真实的 929B 高了 44%
（用户当场指出「韩国海力士也不对啊」）。用「市值 ÷ 合约价」，报价币种、
ADR 折股比例、AB 股结构一次全被约掉，而且乘回现价之后数字还跟着行情实时走。

验证：`TENCENTUSDT`（k=8.90B）与 `HK0700USDT`（k=1.137B）都读出 488B；
`SKHYUSDT`（ADR，929.6B）与 `SKHYNIXUSDT`（韩股，928.5B）差 0.12%。

## 3. 数据从哪里拉

| 用途 | 地址 |
| --- | --- |
| 币的供应量 | Binance apex / 产品表 + CoinGecko（原有逻辑不变） |
| 合约分类与 base | `https://www.binance.com/fapi/v1/exchangeInfo` |
| 合约现价 | `https://www.binance.com/fapi/v1/ticker/price` |
| 公司市值 | `https://stockanalysis.com/<路径>/__data.json` |
| 汇率 | `https://open.er-api.com/v6/latest/USD` |

两条注意：

- **VPS 在美国，`fapi.binance.com` 返 451**，但 `www.binance.com/fapi/v1/*`
  是 200。所以服务端走 `www.binance.com`。（「REST 在 VPS 上不通」这句
  只对 `fapi.binance.com` 成立。）
- stockanalysis 的页面是 SvelteKit 的 devalue 格式：`nodes[].data` 是一个扁平
  数组，`data[0]` 是「字段名 → 下标」的映射，真值要再去数组里取一次。直接把
  `data[0]` 当值读会静默取错。

stockanalysis 的路径按上市地分：

| 上市地 | 路径 | 市值币种 |
| --- | --- | --- |
| 美国 | `stocks/<代码>` | USD |
| 香港 | `quote/hkg/<代码>` | HKD |
| 韩国 | `quote/krx/<代码>` | KRW |
| 上海 | `quote/sha/<代码>` | CNY |
| 未上市 | `private/<slug>` | USD |

未上市那一档没有 `marketCap` 字段，估值在 `statsLeft` 里，取
**Implied Valuation**（二级市场当下的隐含估值），没有它才退回
**Valuation**（上一轮融资的投后估值，发布当天就已经是旧的）。

## 4. 非美股一律要点名，不猜

只有 `underlyingType == EQUITY` 且 base 是纯字母数字时，才允许直接拼
`stocks/<base>`。其余一律要在 `market_meta.rs` 的 `LISTINGS` 里点名映射。

猜的代价是实打实的：`stocks/ANTH` 会命中 AN2 Therapeutics，一家 1.92 亿的
生物科技公司；把它写成 Anthropic 的市值，比留空糟得多。

当前 `LISTINGS`（26 条）：

- 港股：HK0700 / TENCENT → 0700，HK1810，HK0625，HK0992，MEITUAN → 3690，
  KUAISHOU → 1024，POPMART → 9992，BYD → 1211，MINIMAX → 0100，
  GIGADEV → 3986，ZHONGJI → 3308，ZHIPU → 2513
- 韩股：SKHYNIX → 000660，SAMSUNG → 005930，HYUNDAI → 005380，
  SAMSUNGEM → 009150，HANMI → 042700，LGELECTRONICS → 066570，NAVER → 035420
- A 股：CXMT → 688825，UNITREE → 688836
- 未上市：ANTHROPIC → `private/anthropic`，OPENAI → `private/openai`
- 美股但合约名不是代码：BRKB → `stocks/BRK.B`（伯克希尔 B 股），
  QNTX → `stocks/QNT`（QNT 已经是个币，币安改了名）

base 取自 exchangeInfo 那一行的 `baseAsset`，不是从 symbol 尾巴上切。
币安的计价资产有 `USDT USDC USD1 U BTC` 五种，`SPCXUSD1` 只有按行里的
`baseAsset` 才读得出 `SPCX`。

## 5. 全量清点结果（2026-09-18 线上实测）

193 个非币合约：**144 个有市值，49 个留空**。

留空的 49 个，按原因分：

| 原因 | 个数 | 合约 |
| --- | --- | --- |
| 商品，没有公司 | 8 | XAU XAG XPT XPD CL BZ NATGAS COPPER |
| 指数，没有公司 | 2 | BTCDOM ALL |
| ETF / 杠杆 ETP，页面只有 AUM 没有 marketCap | 38 | QQQ SPY IWM SMH SOXL SOXS SQQQ TQQQ TZA TMF TBT UVXY XBI XLE GDX URNM BITO NVDL TSLL KORU EWZ EWY EWJ EWT KODEX200 CSOPSAMSUNG2L CSOPSKHYNIX2L DRAM RAM LYTE MUU MVLL KSTR INTW SNXX STXX SKUU SKDD |
| 认不出这是哪家公司 | 1 | BBX |

AUM 是资产管理规模，不是市值，**绝不能当市值发出去**，所以 ETF 一档是按
设计留空的，不是漏了。BBX 查不到对应的上市公司，按「宁可留空」的规矩空着。

## 6. 抓取失败怎么办

stockanalysis 偶尔会对正常的上市公司回一个挑战页而不是 JSON（实测 Visa 中过一次）。

- 抓失败 → **立刻重试一次**；再失败 → 沿用上一轮刷新的数字，而不是变成空。
- 页面正常打开但读不出 marketCap（ETF 就是这样）→ 那就是真的空，不沿用。
- 汇率或价格表整个拿不到 → 整张股票表沿用上一轮，不做半张。

## 7. 代码位置

- `Backend/kanpan-api/src/market_meta.rs`
  - `Kind`：`Crypto` / `TickerEquity`（美股，代码即地址）/ `NamedEquity`
    （其余，必须点名）/ `Other`（商品、指数）
  - `LISTINGS`、`listing()`、`listing_currency()`
  - `multiplier()`、`money()`、`parse_stockanalysis_cap()`、
    `parse_private_valuation()`、`parse_fx()`、`parse_exchange_info()`
  - `refresh_equities()`：逐个合约抓，间隔 200ms，失败沿用
- 单元测试 47 条，`cargo test --lib`
