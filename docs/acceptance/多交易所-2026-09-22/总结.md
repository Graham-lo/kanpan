# 多交易所 · 总结（2026-09-22 → 09-23）

用户 2026-09-22 拍板：接 Coinbase 现货，并把多交易所能力抽成独立模块（品种身份 / 行情提供者 / 线路三者正交），
后面还要接别家。阶段 1 由 Codex 第二线程在 `multi-exchange` 分支上做，09-23 起由 Claude 子代理接手，
在短命工作树里 rebase 到 main、小步直推，五个阶段全部完成。旧分支 `xchain-tmp`、`xchain-venue`、`venue-tmp`
与工作树 `kanpan-wt-xchain`、`kanpan-wt-xchain-venue` 已删。

## 做了什么

- **身份与无损迁移**（阶段 1，`cba7c93`）：`InstrumentID(venue, market, symbol)`，内部键 `venue/market/symbol`；
  旧自选、画线、提醒、复盘、K 线与持仓量缓存、opens/quotes 全部无损迁到新键，裸符号只作旧数据输入映射到
  `binance/usd_m/`；服务端各白名单接受 `coinbase/spot`。另一路 P3 线程随后补齐提醒、小组件、实时活动、
  Handoff 等处的完整键（`fa72dc1`…`adc3f0b`）。报告：[阶段1](阶段1.md)。
- **行情提供者抽象**（阶段 2，`09a4067`）：`MarketProvider` + 能力位 + `RouteResolver`（直连 / 网关两档 →
  各家自己的主机名）+ `VenueRegistry`；币安原样包成 `BinanceProvider`，行为零变化；
  `Tools/check-venue-isolation.sh` 守着交易所专属代码不漏出各自目录。报告：[阶段2](阶段2.md)。
- **接入 Coinbase 现货**（阶段 3，`daaa3b0`）：客户端 `KanpanNetwork/Coinbase/`（REST 翻页、推送、限速、DTO）；
  kanpan-api `src/venues/coinbase.rs`（白名单透传 + 所有手机共享一路上游的推送 hub）、复盘与提醒按记录自己的
  交易所取数。报告：[阶段3](阶段3.md)。
- **Coinbase 品种的完整界面**（阶段 4，`d7410d8`）：自选里自成「Coinbase」一类、显示 `BTC/USD`、搜索行灰色小字、
  顶栏六格无数据写「—」、现货不画外部指标副图、板块页不收别家品种。报告：[阶段4](阶段4.md)。
- **回归、端到端验收、接入指南**（阶段 5）：见下；指南 [多交易所-接入指南](../../多交易所-接入指南.md)（`0e5f60f`）。

## 验收

- 单测：core 404、data 208、network、app-logic（含隔离检查）、sync-contract、account、chart、kanpan-api cargo 全绿。
- 币安回归（iPhone 17 Pro Max 模拟器）：自选、画线、提醒、复盘、同步往返、线路、搜索共 26 条 UI 用例全过；
  cb7 里红的三组（画线撤销、体验状态清理、复盘）是与多交易所无关的过期用例，已对齐；复盘那次的空白图在当前 main 上复现不出来。见 [阶段5](阶段5.md)。
- Coinbase BTC-USD：1m / 1h / 1w、直连 → 网关 → 直连（以网络诊断里的实际地址为证）、退后台再回来、画线并挂提醒，
  `CoinbaseVenueUITests` 3 条全过。
- 速度：币安首屏中位 1570 → 1089ms（改后更快），推送首帧 914 → 1040ms 两组区间大面积重叠，没有倒退；
  Coinbase 冷启动 K 线逐帧中位 1.2s（币安 0.8s），推送首帧直连约 1.8s、网关约 1.2s。
- 服务端：`daaa3b0` 于 09-23 17:18:37 部署；部署后发现复盘 worker 从 16:54 那次部署起反复 panic（预算时间溢出），
  `e5beef2` 修复并于 17:25:23 部署；此后两服务 active、`NRestarts=0`。18:34 起只读验证 Coinbase 透传两条路径 200、
  白名单外 404。

## 验收中抓到并修掉的

- Coinbase 切到「网关」其实没换线：feed 身份缺了线路（`5909020`）。
- Coinbase 提醒写成「BTC」，和币安永续分不出来：改用 `Alert.name(of:)` 写 `BTC/USD`（`8626d0a`）。
- 复盘 worker 预算时间溢出导致 panic 循环（`e5beef2`）。
- 隔离检查被一行注释里的交易所名打红（`8fce9ad`）。

## 边界

- 用户看得见的文案只放显示代号（`BTC/USD`、`BTC`），内部键只出现在 DEBUG 诊断里；UI 用例每一屏都扫。
- Coinbase 只接美元现货；持仓量、资金费率、结算写「—」，外部指标副图不画。
- 没有真机验收，也没有真的拔网（会断掉整台 Mac），断网重连以「退后台 40s 后恢复」替代。
- Caddy 下 Python 网关的 `/market/` 不认 `source=coinbase`（404）；客户端网关线路走的是 kanpan-api 的 `/v1/market/`，不受影响。
