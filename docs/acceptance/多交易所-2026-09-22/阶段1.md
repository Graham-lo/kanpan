# 多交易所 阶段 1：身份与无损迁移

日期：2026-09-22。工作树：`/Users/mdd/zhk/kanpan-coinbase`；分支：`multi-exchange`。

## 改动

新增 `InstrumentID(venue, market, symbol)`，内部键统一为 `venue/market/symbol`。裸符号仅作为旧数据输入，映射到 `binance/usd_m/`。`SymbolInfo.id`、K 线序列、报价簿、自选、画线、提醒、复盘与同步使用同一身份；交易所接口仍发送各自的裸符号。

自选由 `kanpan.symbols.v1` 读取、以 v2 写入；保留旧值供迁移追溯，显式清除时同时移除两版，防止旧值复活。画线混合旧键与新键按 drawing ID 合并。K 线缓存迁入 `series/<venue>/<market>/`，OI 缓存按完整身份分目录；旧文件可读且迁移写入不删除旧文件。opens/quotes 归档恢复时归一身份。服务端自选、画线、提醒、分享与复盘白名单接受 `coinbase/spot`，身份与线路解耦。

## 假设与边界

- `.project-memory/CODEX.md` 在现行仓库不存在，以 AGENTS 指定的 `PROJECT.md` 为当前记忆。
- 阶段 1 保留现有行情提供者，Coinbase 实际取数与服务端提醒行情接入留在阶段 3，界面能力适配留在阶段 4。
- `SymbolInfo.symbol` / `BarSeries.symbol` 等兼容字段的内容改为完整键；接口与显示需要裸符号时用 `InstrumentID.symbol`，避免同名市场串数据。
- `KanpanChart` 依赖 UIKit，按现有 Makefile 通过 iOS `xcodebuild test` 验证，不将 macOS `swift test` 的平台错误当作产品失败。
- 本机有其他线程同时构建和运行模拟器。本线程使用独立 DerivedData、独立模拟器、串行编译，未操作其他线程的进程或设备。

## 验证记录（合入前复查进行中）

| 命令 | 退出码 / 结果 |
| --- | --- |
| `swift test --package-path KanpanCore -j 1 -Xswiftc -disable-batch-mode` | 0；364 条 Swift Testing + 4 条 XCTest |
| `swift test --package-path KanpanNetwork -j 1 -Xswiftc -disable-batch-mode` | 0；96 条 |
| `swift test --package-path KanpanData --scratch-path …/KanpanNetwork/.build --disable-build-manifest-caching -j 1 -Xswiftc -disable-batch-mode` | 0；186 条 |
| `cd Backend/kanpan-api && cargo test --lib` | 0；155 条 |
| App build-for-testing / 受影响 UI | 0；独立 iPhone 16 Pro（iOS 26.5），3 条通过，128.749s |
| `xcodebuild test -scheme KanpanChart`（iOS 26.5，超时 120/300s） | 0；126 条 Swift Testing + 4 条 XCTest |
| `make app-logic-test CORE_TEST_FLAGS="… --disable-build-manifest-caching -j 1 -Xswiftc -disable-batch-mode"` | 0；8 个逻辑包共 442 条；另加同步往返用例后 AccountCodec 14 条通过（总计 443） |
| `xcodebuild test -scheme KanpanReview`（iOS 26.5） | 0；旧基线全通过，合并后复查见下 |

共享 SwiftPM 构建产物时显式关闭 build-manifest caching，并核对实际测试 runner；一次未关闭缓存的尝试跑成 Core，已作废，没有冒充 Network 结果。

迁移测试覆盖旧自选全部字段、旧画线混合键合并、旧提醒触发状态、旧 SymbolInfo JSON、旧 K 线二进制、opens/quotes 和旧 OI 文件保留，及两家交易所缓存隔离。修复测试发现的 REST ticker/OI 裸符号比较遗漏。

## 合并主线后的复查

已 rebase 到 `d4f5e2f`，保留 P1 的三种副图、盘口与归档扩列，以及提醒铃声线程的两阶段改动。五处冲突仅合并身份归一与主线新增逻辑，没有重写其他线程的功能。新增盘口 DTO 的内存身份、统计 REST 的裸符号/响应匹配也纳入归一；扩列请求保留 `metrics=1`。

- Network：106 条通过，退出 0；三个统计接口另扩展了裸符号/完整身份参数化断言，待补跑。
- Data：191 条通过，退出 0。
- Rust：157 条通过，退出 0。
- 其余复查进行中。
- 冷/热缓存证据区分：`阶段1-初测行情.log` 是旧基线首次运行（624ms）；`阶段1-复查热缓存行情.log` 明确命中快照（16ms）。macOS 的 `NSTemporaryDirectory` 没有按此进程设置的 TMPDIR 切目录，后者不能当作冷启动对照。阶段 2 将在测量工具中显式指定缓存根。

## 后端部署

部署目标：`orderflow-vps:/opt/kanpan-api`。部署前比对文件 SHA，保留远端其他线程已部署的 LSR/TAKER/BASIS、alertSound、depth 规则，未将其夹带进本线程提交。备份：`/opt/kanpan-api/backup-multi-stage1-20260922-144403`（旧二进制及本次涉及的源码）。

先 rsync 暂存、加部署锁、校验指纹，再应用本线程差异。额外远端 `cargo test --release --lib` 退出 101：部署目录只有后端，既有测试的 `include_str!(../../../KanpanCore/…/Interval.swift)` 缺少 Swift 源文件；本地完整仓库的 155 条 Rust 测试通过。随后 `cargo build --release`、`python3 ops/install.py`、`systemctl restart kanpan-api kanpan-worker` 均成功。

2026-09-22 15:00:08 CST 两服务启动，`ExecMainStatus=0`。重启后立即 curl 曾因端口尚未监听退出 7，随后复查 `/health` 返回 `{"data":{"ok":true}}`。本机及公开 `/v1/capabilities` 均返回 `binance/usd_m` 与 `coinbase/spot`。公开根路径 `/health` 未配置转发、返回 404，不改 Caddy。已有股票供应量来源不匹配警告仍由现行逻辑跳过，没有改动该任务范围。

2026-09-22 拉取到 `95c745c` 后获知新的 AGENTS 部署纪律：先合入并推送，再从 `origin/main` 部署。上述部署发生在读取该新增规则之前；后续部署遵守新规则。

## 提交、合入与截图

原始截图已导出并逐张查看：

- [冷启动自选](阶段1-ui/冷启动自选.png)：BTC/ETH 报价与涨跌正常，界面未显示内部身份键。
- [画线重启恢复](阶段1-ui/画线重启恢复.png)：射线及颜色、锁定状态恢复。
- [UI 汇总](阶段1-ui/summary.txt)：3 条通过，脚本退出 0。

## 接手收尾（2026-09-23，venue）

阶段 1 由接手线程在 `/tmp/venue`（`venue-tmp`，短命）收尾，没有新开长命分支。

- 合入 `origin/main` 的 P3.1（价格 / 复盘到点 / 自选五分钟波动三种提醒）与画线画法记忆时，
  把它们新写的代码也归到完整身份上：`AlertEngine` 的盯价清单与测试注入按 `InstrumentID.canonical`
  比较；`AlertStore.addPrice` 去重用完整键；`WatchMove.Tracker` 的闸键改成「完整键|方向」
  （完整键里本身带 `/`）；`WatchMoveMonitor` 的自选与喂价一律归一；新建价格提醒页认得
  `BTC/USD`、`BTC-USD`，并显示 `BTC/USD` 而不是内部键。新增单测「自选没拿掉的品种闸照留：品种键里带「/」」。
- 复查（旧基线上的阶段 1 提交，日志 `/tmp/venue-logs/s1b`）：app build、core、network、app-logic、
  account、cargo、chart、review、主工程逻辑、`make sync-contract`（退出 0、工作树无漂移）全部退出 0；
  data 一步在本机负载 19～27 时有 10 处计时断言失败（`StoreTests` 20ms 门限、WS 回放退避 / pong /
  60s 重连、封禁补洞），均为时间窗类，负载降下来后在最终基线上重跑，结果记在 `阶段2.md`。
- rebase 到最新 `origin/main` 之后，阶段 1～2 作为一组在阶段 2 的提交上整套复查后推送，命令与退出码见 `阶段2.md`。
- **不依赖 `/v1/capabilities`**：上面「后端部署」一节里用它只是当时的只读探针；客户端从阶段 1 起
  就不读这个接口，交易所与能力全部在本地的 `VenueRegistry` / `MarketCapabilities` 里定死，
  P4.11 删掉它不影响多交易所。

