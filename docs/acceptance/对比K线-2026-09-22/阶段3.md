# 对比 K 线 · 阶段 3：交互收口（2026-09-23）

状态：已完成。交互代码随阶段 2（`d781195`「对比K线阶段二」）一并上了 main，本阶段是验收：在 iPhone 17 Pro Max 模拟器上把规格 §1「对比时」那几条逐条走一遍，截图与读数入库。执行：Claude 子代理 xchain（Opus 5.5，high）。

## 逐条核对

| 规格 | 怎么验 | 结果 |
| --- | --- | --- |
| 对比时主图指标不画 | 读数 `overlays == []`；截图主图只剩蜡烛 + 三条折线，没有均线 | 通过（[1m](阶段3/BTC-ETH-SOL-DOGE-1m.png)） |
| 对比时画线全部隐藏、底栏「画线」不可用 | `drawingsVisible == false`，`bottom.draw` 不可点（截图里底栏第一格变灰） | 通过 |
| 三档周期 | BTC 主 + ETH/SOL/DOGE，1m / 1h / 1d 各等到三条都可画（`compareReady == 3`，≥100 根） | 通过（[1m](阶段3/BTC-ETH-SOL-DOGE-1m.png)、[1h](阶段3/BTC-ETH-SOL-DOGE-1h.png)、[1d](阶段3/BTC-ETH-SOL-DOGE-1d.png)） |
| 十字线读数与右轴一致 | 十字线切到「收盘价」，长按图上一点；`assertPercentReadout` 比图例百分比、右轴刻度与主序列算出来的值 | 通过（[十字线](阶段3/对比-十字线.png)） |
| 平移后重定基点 | 向右拖 170pt，`compareBaseTime` 变了，0% 落回可视最左那根的开盘价 | 通过（[平移](阶段3/对比-平移重定基点.png)） |
| 横屏画线工作台互斥 | 转横屏：`percentAxis == false`、`compareKeys == []`，均线回来；转回竖屏三条对比自动恢复 | 通过（[横屏](阶段3/对比-横屏暂退.png)、[竖屏恢复](阶段3/对比-竖屏恢复.png)） |
| 复盘录制不含对比 | 图表面板「记一笔」：录制态 `percentAxis == false`、`compareKeys == []`；收起后三条对比恢复 | 通过（[复盘](阶段3/复盘-不含对比.png)） |
| 扫图换主品种时集合保留，等于主品种的忽略 | 自选 BTC→ETH 横滑：图上只剩 SOL/DOGE，颜色仍是原槽位的粉 / 绿；面板里 ETH 的「移除」还在；滑回 BTC 三条全回来 | 通过（[扫到 ETH](阶段3/对比-扫到ETH忽略自身.png)、[扫回 BTC](阶段3/对比-扫回BTC集合保留.png)） |
| 集合持久 / 清除 / 上限 3 | 加满 3 只「添加」置灰，重复与主品种不进集合，重启仍在，「清除对比」后回价格坐标 | 通过（[三项面板](阶段3/对比-三项面板.png)） |
| 跨设备同步 | 自造账号注册 → 立即同步 → 换全新本地档案登录 → 三只原样回来 | 通过（[新档案恢复](阶段3/对比-新安装档案登录恢复.png)）；前提是线上服务端已有 `compareSymbols` 白名单（09-23 16:27 部署） |
| 文案只露代号 | 图例写 `BTC / ETH / SOL / DOGE`，横屏顶签 `BTCUSDT`，面板行名为 base；没有 `binance/usd_m/…` | 通过 |

## UI 用例

`CompareUITests`（4 条）在 iPhone 17 Pro Max 上全过，结果见 [UI结果.log](阶段3/UI结果.log)。本阶段修了一处用例问题：面板里「十字线」那一行在下沿之外，原来的 `scrollViews.firstMatch.swipeUp()` 一划越过它，改成只拖 `panel.content`、落进可视区就停（app 未改）。

`阶段3/合入前/` 是分支期（rebase 到 main 之前）同一套用例的截图，留作对照。
