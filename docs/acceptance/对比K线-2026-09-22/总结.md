# 对比 K 线 · 总结（2026-09-22 → 09-23）

用户 2026-09-22 拍板要做（此前在不做清单里）。先交 Codex 第三线程在 `compare-kline` 分支上做阶段 1–2，09-23 起由 Claude 子代理接手，rebase 到 main、小步直推，四个阶段全部完成。旧分支 `compare-kline`、`compare-kline-s2` 与工作树 `/Users/mdd/zhk/kanpan-compare` 已删，分支上没入库的验收材料先搬进了 main。

## 做了什么

- **画法**（阶段 1，`fed4705`）：`ChartState.compare` / `percentAxis`；主图换成百分比坐标，基点 = 可视区最左那根的开盘价，平移缩放时重定；主品种画蜡烛，对比品种画收盘价折线；图例、右轴 `±x.xx%`、0% 刻度。
- **数据与入口**（阶段 2，`d781195`）：`KanpanData/.../Feed/CompareFeed.swift` 拉历史并订阅末根，按 openTime 对齐主序列；`Kanpan/Kanpan/Compare/CompareModel.swift` 管生命周期；「图表」面板「对比」一节（添加 / 移除 / 清除，上限 3）；`Prefs.compareSymbols` 随账号同步，客户端与服务端白名单两边对账，服务端已部署。
- **交互收口**（阶段 3，代码随阶段 2 上线，09-23 验收）：对比时主图指标与画线隐藏、底栏画线置灰；横屏画线、复盘记一笔、看朋友的线时暂退、回来恢复；扫图时集合保留、等于主品种的那只临时不画。
- **文档**（阶段 4）：使用手册「对比」一节、PROJECT.md。

## 验收

- 模拟器（iPhone 17 Pro Max）：BTC 主 + ETH / SOL / DOGE，1m / 1h / 1d 三档；十字线读数、图例与右轴同口径；平移后基点重定；横屏与复盘暂退后恢复；扫图保留集合；加满、重复、清除、重启；自造账号注册后换全新档案登录，集合原样恢复。`CompareUITests` 4 条全过。见 [阶段3](阶段3.md)。
- 冷启动：带三条对比与不带对比的主图首屏中位数 14.71s 对 14.35s，在单组波动之内；对比请求在主图首屏之后才发，不占首屏等待链。见 [阶段2](阶段2.md)。
- 服务端：`compareSymbols` 白名单 09-23 16:27 从 `origin/main` `ae7673a` 部署上线；部署前「新档案登录恢复」用例失败正是因为线上缺这条白名单，部署后通过。

## 边界

- 显示只用代号（图例 `BTC / ETH …`），不露 `binance/usd_m/…` 这类内部 key；读屏也改成念代号。
- 对比颜色不让用户选，最多 3 只，不做价差 / 组合 K 线，也不在板块页做固定起点比较（不做清单）。
- 没有真机验收：真机签名坏了，按规矩只在模拟器上验。
