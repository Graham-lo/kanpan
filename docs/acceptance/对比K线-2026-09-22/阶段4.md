# 对比 K 线 · 阶段 4：文档收尾（2026-09-23）

状态：已完成。只动文档，不改代码。

- `docs/使用手册-2026-09-21.md`：「图表设置」下新增「对比」一节——入口、上限 3 只、百分比坐标与基点、图例与十字线、对比期间哪些东西暂退、扫图时的行为、随账号同步。按现行源码写（`Kanpan/Kanpan/Main/MainScreen.swift` 的 `comparing` / `compareKeys`、`Kanpan/Kanpan/Panels/ChartPanel.swift` 的「对比」一节），和阶段 3 的模拟器验收逐条对得上。
- `.project-memory/PROJECT.md`：「对比 K 线 · 第三线程」一节记四个阶段都已完成、旧分支与工作树已删，并链到阶段 3、阶段 4 与总结。
- `总结.md`：四个阶段的提交、验收、部署与边界放在一页里。
