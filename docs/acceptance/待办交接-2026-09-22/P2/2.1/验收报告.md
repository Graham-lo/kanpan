# P2.1 · 删 `SymbolTickerFeed` 死代码

## 改动

- 删掉 `Kanpan/Kanpan/Symbols/SymbolTickerFeed.swift`，以及测试壳 `Kanpan/Symbols/Sources/KanpanSymbols/` 里指向它的符号链接。
- `SymbolPickerModel` 去掉 `feed` 参数、`log` 钩子、`subscribed`、`subscribe()` 和 `disappear()`。报价一直由宿主的 `QuoteBook` 灌进来，模型自己从没有生产调用方传过 `feed`。
- `FavoritesView`、`SymbolPickerView` 删掉对 `model.disappear()` 的调用，那本来就是空操作。
- 两个预览宿主（`SymbolPickerView`、`SymbolSearchView`）改用新增的 `QuoteBook.preview(_:)` 静态桩：只装报价，不连网、不写缓存。
- `SymbolPickerModelTests` 删掉两条只测订阅/退订日志的用例，其余照旧。

## 验证

| 项 | 命令 | 结果 |
|---|---|---|
| 品种包 | `make symbols-test` | 157 条 / 23 组全绿，`logs/symbols-test.log` |
| 其余 app 逻辑包 | `make sector-test settings-test diag-test account-codec-test deeplink-test scan-test alerts-test`（`app-logic-test` 除 symbols 以外的部分） | 见 `logs/app-logic-rest.log` |
| 应用编译 | `xcodebuild build -workspace Kanpan.xcworkspace -scheme Kanpan -destination generic/platform=iOS Simulator`（2026-09-23 在 origin/main 6b845f7 上重放后重编） | BUILD EXIT 0 |
| 残留引用 | `grep -rn SymbolTickerFeed`（排除 docs 与 .build） | 0 处 |

## 假设

- 被删的两条模型用例测的是「出页面后退订」这条死路径本身，路径没了用例一起删；A5.7 真正的退订行为由 `QuoteBook` 负责，它的用例不动。
