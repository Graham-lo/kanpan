# P1 · 外部统计指标与盘口

状态：P1初版已重放到最新 main 并推送 `d4f5e2f`（原本地提交 `5742db3`）。Claude验收三个副图通过，盘口视觉打回：按价位画的细线在BTC上不可见，自动测试只数数据档位不足以验收。返工记录见 `P1/rework-1/`。

**后续（2026-09-24 补记）**：返工 1 `47d3705`（固定十行梯）经 Claude 验收通过；返工 2 `d27f5e5`（柱长开方缩放、2pt 下限）见 `P1/rework-2/验收报告.md`。P4.1 矩阵重跑时 `testDepthLadderVisibleAtBothIntervals` 在 iPhone 16 Pro 上按完整品种 key 修正用例后通过（`docs/acceptance/M8.md`）。盘口现为通过状态，本段以下为初版记录。P0.5 已推送 `946f653`；本阶段接管其遗留源码，保留其它线程新增的独立交接书和提交，不包含旧 M8 日志。

## 已实现的路径

- 多空比取全市场账户数比，主动买卖比取主动买量/主动卖量，基差取基差率百分数；三个副图单线，候选共十种，同时最多三种。界面名称、参数名和图例为中文，原存档标识保持不变。
- 四个统计端点共用 `futures/data` 的 1000 次/5 分钟限额。旧持仓量调用同一额度桶。
- 日切片升级为 KOI3、聚合段升级为 KOI4，每点44字节保留持仓量及四个比率；旧 magic 未命中重取。服务端日缓存用 `.metrics.json`，旧两列文件保留，按需请求时重取。自动预热仍认旧文件，部署不会触发全量归档重抓。
- 归档接口增加 `metrics=1` 的六列响应；不带参数仍返回两列，兼容已安装客户端。多空比、主动买卖比读取对应历史列；基差仅查近30天。
- 初版盘口默认关，开关位于图表设置；初版按价位绘制未通过视觉验收。盘口不影响主图范围、布局和轴宽，仅使实时绘制层失效。
- 逐笔流只维护当前统计桶，不建逐笔列表。切品种、重连和退出行情时清除旧尾巴与盘口；订阅跟随开关。逐笔向界面最多每100毫秒更新一次。
- 网关保留已选副图并显示对应中文空态，不切线路、不发不支持的统计请求。盘口为空。
- `depth` 是同步偏好；新增指标标识与字段经 `make sync-contract` 对账到服务端白名单和值规则。
- 接续 `89c134c` 的 P0 小尾巴：头部涨跌额与价格共用千位分隔，四位整数也分组。

## 判断与取舍

1. 中途接入及重连后第一段成交不是整桶：保留最近完整REST桶，从连接后第一个自然采样桶开始给实时尾巴；分母为零时不给假比率。小于5分钟的图采用5分钟采样，超过日线的图按日采样后取桶末值，与历史对齐一致。不给用户算法选择。
2. 本阶段按交接书P1的新要求恢复网关空态，替换此前“隐藏持仓量那一格”的旧呈现。
3. 20次模拟器切换验收采用BTC与ETH交替20次，逐次核对图表与盘口品种并保存订阅日志，不扩展P0的四只验收对象。
4. 文档中第二、三、四线程的多交易所、对比K线、铃声任务由其独立工作树处理，本线程继续P1→P4。

## 验证记录

- `make sync-contract`：已生成契约并通过生成一致性测试。
- `swift test --package-path KanpanNetwork`：106条通过。
- `swift test --package-path KanpanData`：扩列前的旧大小断言失败，保留日志；更新格式断言及新增测试后首轮188条通过。补充订阅切换与旧代理拒收后189条中有两条耗时断言失败（快照55.78ms超过20ms、首屏3秒等待超时）；保留失败；未放宽断言，降低本轮构建并发后完整复跑189条全部通过（9.367秒），见 `P1/logs/data.log`。
- `swift test --package-path KanpanCore --jobs 4`：首次发现5个旧英文线名断言，已按中文规则更新；最终366条Swift Testing及4条XCTest通过。
- `cargo test --lib`：154条通过，包含新响应的六列往返与旧客户端两列兼容。
- 图表模拟器单测：128条Swift Testing及4条XCTest通过（`P1/logs/chart.log`），包括外部输入独立刷新、基差百分比、盘口不改变几何且只刷实时层。
- 完整应用编译及双架构测试包构建通过；主界面模拟器33条、应用逻辑441条全部通过。受影响UI最终1条通过、0失败（429.397秒；含启动/收尾的脚本耗时555秒），见 `P1/ui/summary.txt`。

### 界面首轮失败保留

第一轮在默认成交量开关的定位处失败，尚未执行新副图与盘口断言。控件树中开关坐标为`y=-845.7`，面板可视区从`y=467.8`起：连续大幅上滑越过了控件。改为按控件当前位置选择上下方向，每次拖半个可视区；没有放宽业务断言。原始日志、控件树与截图保存在`P1/ui-first/`。本用例包含20次品种切换，显式上限900秒，沿用脚本的最大超时。

第二轮三副图真实曲线与盘口10档均通过并截图；在第一次切品种时发现测试沙盒自选默认为空。给这条用例显式设置隔离目录与BTC/ETH自选夹具，并通过真实底栏进图表、经自选行切品种。没有注入行情值或盘口数据。日志与控件树保留在`P1/ui-second/`。

## 服务端

备份 `/opt/kanpan-api/backup-p1-20260922-1408/`，包含运行二进制与源码/契约/迁移/安装脚本归档。构建前同步 `src/`、`contract/` 并 `touch src/*.rs`；发布构建、安装、显式重启已完成。API于14:08:25 CST、worker于14:08:24 CST启动，均active；内网健康200、公网元数据与能力接口200。相同BTC历史范围，新请求返回六列（账户比1.27565999、主动买卖比2.457464），旧请求返回两列，均200。没有修改Caddy或Python网关配置，没有新增迁移或删除线上个人数据。

新二进制SHA-256：`1f0329db7313693b602e1970d515eba10b8413f67af5757b1d43746c12089dc6`。契约SHA-256：`98f58055c172b8fad44bcd3a162c7a258cf43a36f555aed6fd0d71ba6ca5932e`。部署与只读输出原文分别保留在 `P1/logs/backend-deploy.log`、`P1/logs/backend-readonly.log`。

日志目录：[P1/logs](P1/logs/)。截图目录：[P1/screenshots](P1/screenshots/)。所有结果仅为模拟器，不是真机验收。

## 执行命令

```sh
make sync-contract
swift test --package-path KanpanCore --jobs 4
swift test --package-path KanpanNetwork --jobs 2
swift test --package-path KanpanData --jobs 2
make app-logic-test CORE_TEST_FLAGS='--jobs 2'
(cd Backend/kanpan-api && cargo test --lib)
xcodebuild build -workspace Kanpan.xcworkspace -scheme Kanpan -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/kanpan-p1-dd CODE_SIGNING_ALLOWED=NO
(cd KanpanChart && xcodebuild test -scheme KanpanChart -destination 'platform=iOS Simulator,id=8C9E429F-4135-4D82-BFD6-6AF577B999FA' -derivedDataPath /tmp/kanpan-p1-chart-dd -jobs 2 -test-timeouts-enabled YES -default-test-execution-time-allowance 180 -maximum-test-execution-time-allowance 300)
(cd Kanpan/KanpanTests && xcodebuild test -scheme KanpanMain -destination 'platform=iOS Simulator,id=8C9E429F-4135-4D82-BFD6-6AF577B999FA' -derivedDataPath /tmp/kanpan-p1-main-dd -jobs 2 -test-timeouts-enabled YES -default-test-execution-time-allowance 180 -maximum-test-execution-time-allowance 300)
ONLY_DEVICE='iPhone 16 Pro' SUFFIX=' · p1-ui' ONLY_TESTING='KanpanUITests/ChartFoundationUITests/testExternalIndicatorsAndDepthRoundTrip' DD=/tmp/kanpan-p1-dd RES=/tmp/kanpan-p1-ui-results OUT=docs/acceptance/待办交接-2026-09-22/P1/ui Tools/ui-test.sh
```

两个独立的iPhone 16 Pro模拟器均为iOS26.5，分别用于主界面单测与UI操作，避免共用设备互相卸载。KanpanChart依赖UIKit，macOS裸`swift test`不适用，因此用同一测试包的iOS模拟器目标执行。

## 模拟器最终视觉与订阅证据

- [三副图曲线](P1/screenshots/三副图-多空比-主动买卖比-基差.png)：三个新指标同时显示真实曲线，基差读数为百分数。
- [盘口开启](P1/screenshots/盘口-买五卖五.png)：收到当前BTC的买五卖五共10档，量柱锚在价位、贴价格轴左侧。BTC五档只相差几个报价步长，在正常价格范围会落入相近像素；没有人为拉开价位或扩大坐标轴。
- [网关空态](P1/screenshots/网关-三副图空态.png)：三格保持、名称对应，盘口清空。
- [盘口关闭](P1/screenshots/盘口关闭-回到普通图表.png)：切回直连后恢复，再关闭到0档。
- [20次切换记录](P1/logs/连续切换20次-订阅与盘口品种一致.txt)：20条逐次断言，BTC/ETH交替，图表品种与盘口品种始终一致；日志含当前组合流的增退订及HTTP结果。
- [最后订阅日志](P1/logs/三指标与盘口-订阅日志.txt)。截图均为iPhone16Pro模拟器原始图，已逐张检查；不是拼接或修图。
- `P1/logs/source-sha256.txt`记录测试时44份源码、契约和测试文件摘要。


## 合回 main 的复核（2026-09-22）

原P1提交依次重放至铃声阶段1、阶段2和两份验收文档，最终 `d4f5e2f` 已以 fast-forward 推送；没有覆盖铃声同步字段、UI接线或旧M8日志。合入后的应用模拟器构建通过，Core366+4、Network106、Chart128+4、应用逻辑445、Rust155通过；Data189条中188通过，既有快照读取耗时用例在高并发构建下26.07ms超过20ms，未改阈值，单独复测通过。初版完整Data189全绿证据仍保留。命令与复核原始输出见 `P1/rework-1/logs/p1-integrated-*.log`。

15:18的线上只读复核仍为元数据、能力、新六列/旧两列归档请求全部200。期间其它线程已部署其合并版本，服务启动时间为15:00:08 CST，不能把14:08那次二进制散列当作线上最新值；新线上二进制与源码散列记录在 `P1/rework-1/logs/p1-integrated-backend-sha.log`。线上保留P1扩列及白名单；其中同步验证源码还含另一线程的品种身份改动，本线程未回滚或覆盖。本次盘口返工只有客户端绘制，无后端部署。
