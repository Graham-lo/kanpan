# P0 · 行情页头部验收

提交号：`fd6a8f6`（代码与回归用例，已推送 `origin/main`）。本报告与截图是该提交的配套证据。

基准：`bd4e822`；相对 `9aa6372` 仅增加另一窗口的桌面原型，P0 未改动该原型。本阶段已完成模拟器验收，未做真机验收。

## 做了什么

- 删除按宽度分档及药丸：只有左侧价格 / 涨跌小字、右侧两列三行统计这一种布局。保留三个可访问标识和整块价格区的扫图手势。
- 价格 22pt medium；涨跌行 13pt semibold，涨跌额与涨跌幅之间两个空格，沿用涨跌文字色。六格标签 12pt、值 13pt semibold，列间距 14pt、格内间距 6pt、行距 3pt；缺数统一 `—`，不足一分钟 `<1分`。
- REST `priceChange`、单品种与全市场流 `p` → `Ticker.priceChange` → `QuoteState` / `QuoteBook.raw` → 头部。较新成交只补统计之后的价差；涨跌额变化会触发报价显示更新。快照扩展为可选字段，兼容旧文件。
- 头部的涨跌额和涨跌幅配对使用 24 小时统计；自选表的口径选择与药丸不变。
- `Tools/ui-test.sh` 增加单机和指定用例过滤，空参数保留完整矩阵；修复中文紧贴变量名导致失败分支汇总中断的问题。既有 M8 日志不属于本次提交。

## 假设与实测差异

- 原轮把闪迪、美光的下单字段精度当成展示精度，显示成四位整数加五位小数；这是错误口径，已由下方「返工 1」修正为按报价步长显示两位。旧截图保留作为返工前证据。
- `.xLarge` 封顶在 390pt 的 1000SATS 上仍超宽 16pt：价格左缘从 12pt 变成 4pt，统计右缘从 378pt 变成 386pt。依据 P0 D.13 下调一档，最终封顶为 **`.large`**，不引入分档、缩字或截字。
- 按最终规格的 12/13pt 字号和 3pt 行距，六格实际高度为 **53pt**；任务书“约 48pt”是旧字号的估值。保留指定字号和内边距，四只品种的头部高度与周期条位置一致，最大系统字号也不再额外长高。相对旧的已掉行头部减少约 31pt，相对旧的正常并排头部多 5pt。
- 为证明 P0 可以独立编译，从 `9aa6372` 建立并更新到 `bd4e822` 的 `/tmp/kanpan-p0-check` 验收工作树，只放 P0 变更。主工作树接管的 P1 开头及两个编译修复留待 P1 提交。

## 修前证据

使用 `Tools/ui-test.sh` 的单机过滤执行新增几何用例，四品种顺序为 SNDK、MU、1000SATS、BTC。旧实现产生 12 条预期断言失败；前三只统计左缘均为 12pt（在价格下方），周期条起点为 200.33pt，BTC 为 164.33pt。

| 闪迪修前 | 美光修前 |
| --- | --- |
| ![闪迪修前](P0/screenshots/16pro-before-SNDKUSDT.png) | ![美光修前](P0/screenshots/16pro-before-MUUSDT.png) |

原始复现图：[1000SATS](P0/screenshots/16pro-before-1000SATS-original.png)。

## 验证命令与结果

下列命令在 P0 独立工作树执行。命令日志均保存在 [P0/logs](P0/logs/)。

| 命令 | 结果 |
| --- | --- |
| `xcodebuild build -workspace Kanpan.xcworkspace -scheme Kanpan -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/kanpan-p0-dd -jobs 2` | 退出 0，BUILD SUCCEEDED |
| `swift test --package-path KanpanCore -j 2` | 360 条通过 |
| `swift test --package-path KanpanNetwork -j 2` | 96 条通过 |
| `swift test --package-path KanpanData -j 2` | 184 条通过 |
| `swift test --package-path KanpanChart -j 2` | macOS 目标无法导入 UIKit；这不是图表测试通过的证据，实际验证用下一行 |
| `cd KanpanChart && xcodebuild test -scheme KanpanChart -destination 'platform=iOS Simulator,name=iPhone 16 Pro · m22' -derivedDataPath /tmp/kanpan-p0-chart-dd -jobs 2 -test-timeouts-enabled YES -maximum-test-execution-time-allowance 180` | 退出 0，125 条 Swift Testing + 4 条 XCTest 通过 |
| `make app-logic-test` | 退出 0，八包共 441 条通过 |
| `cd Kanpan/KanpanTests && xcodebuild test -scheme KanpanMain -destination 'platform=iOS Simulator,name=iPhone 16 Pro · m22' -derivedDataPath /tmp/kanpan-p0-main-dd -only-testing:KanpanMainTests/HeaderStatsTests -test-timeouts-enabled YES -maximum-test-execution-time-allowance 120` | 退出 0，头部逻辑 15 条通过 |
| `cd Backend/kanpan-api && cargo test --lib -j 2` | 退出 0，153 条通过 |

模拟器 UI 命令（测试脚本在实际运行时复制为同目录的固定快照 `Tools/p0-run.sh`，防止运行期间修改脚本影响解释器）：

```sh
ONLY_DEVICE='iPhone 16 Pro' \
ONLY_TESTING='KanpanUITests/MainScreenUITests/testHeaderStatsStayRightOfPrice,KanpanUITests/MainScreenUITests/testHeaderStatsAtLargestDynamicType' \
DD=/tmp/kanpan-p0-dd OUT=/tmp/kanpan-handoff-checks/p0-final-16pro \
RES=/tmp/kanpan-handoff-checks/p0-final-16pro-results bash Tools/ui-test.sh

xcrun simctl spawn ACAF005F-5561-4C64-836B-45E4FB3C5C72 defaults write -g UIAccessibilityBoldTextEnabled -bool YES
xcrun simctl spawn ACAF005F-5561-4C64-836B-45E4FB3C5C72 defaults read -g UIAccessibilityBoldTextEnabled
# 输出 1；随后用例重启 app。
ONLY_DEVICE='iPhone 17e' \
ONLY_TESTING='KanpanUITests/MainScreenUITests/testHeaderStatsStayRightOfPrice,KanpanUITests/MainScreenUITests/testHeaderStatsAtLargestDynamicType' \
DD=/tmp/kanpan-p0-dd OUT=/tmp/kanpan-handoff-checks/p0-final-17e-bold \
RES=/tmp/kanpan-handoff-checks/p0-final-17e-bold-results bash Tools/ui-test.sh
```

两条命令均退出 0，各 2 条 UI 用例通过。每条分别深链打开 SNDK、MU、1000SATS、BTC；等待价格、市值、成交额、费率实值到齐，再断言六格在右侧、涨跌行在价格正下方且左对齐、左右 12pt 留白仍在、涨跌行与六格相隔至少 8pt，以及四品种的六格高度与周期条位置相等。最大字号用例通过系统启动参数 `UICTContentSizeCategoryAccessibilityXXXL` 请求，并额外断言价格 / 六格高度与默认档相同。

| 最终机型 / 设置 | 六格高度 | 周期条起点 | 结果 |
| --- | --- | --- | --- |
| iPhone 16 Pro，默认字号 / 最大字号 | 四只均 53pt | 四只均 169.33pt | 2/2 通过 |
| iPhone 17e，粗体 + 默认字号 / 粗体 + 最大字号 | 四只均 53pt | 四只均 154.33pt | 2/2 通过 |

iPhone 17e 未开粗体的默认字号也已通过一次；该轮的 `.xLarge` 探测失败单独保留在 `xLarge-probe-17e.log`，由最终 `.large` 复验收口，未将失败算成通过。

## 修后截图与参照

| 闪迪修后 | 美光修后 |
| --- | --- |
| ![闪迪修后](P0/screenshots/16pro-after-default-SNDKUSDT.png) | ![美光修后](P0/screenshots/16pro-after-default-MUUSDT.png) |

| Hkline 闪迪（修后） | AICoin 原始参照 |
| --- | --- |
| <img src="P0/screenshots/16pro-after-default-SNDKUSDT.png" width="390"> | <img src="P0/screenshots/aicoin-reference.png" width="390"> |

骨架一致：左侧大字价格及紧贴下方的涨跌小字，右侧数据格；Hkline 保留自己的六格字段和既定取数口径。截图为模拟器 / 用户参照的原始文件，未重绘数字或裁掉边界。

其余截图：`P0/screenshots/16pro-after-{default,AX3}-<symbol>.png` 与 `P0/screenshots/17e-after-{bold-default,bold-AX3}-<symbol>.png`，每组包含四只品种。最窄屏、粗体、最大字号同时开启的 [1000SATS](P0/screenshots/17e-after-bold-AX3-1000SATSUSDT.png) 仍保留两侧留白；失败的 `.xLarge` 对照另存 `17e-xLarge-AX3-1000SATSUSDT.png`。

## 后端与真机

本阶段没有后端代码、数据库或网关改动，不涉及部署；`cargo test --lib` 153 条通过。没有将本地编译当成线上部署或真机验证。按用户要求，真机安装与账号、密钥检查均未进行。

## 返工 1 · 全应用价格按报价步长展示

依据交接书 `6cab4cf` 的返工要求，基于 `71bd340` 完成。返工代码、用例及本节证据在同一个独立提交中；提交可用 `git log -1 --format=%h -- KanpanCore/Sources/KanpanCore/Model/SymbolInfo.swift` 查询。本节取代原轮关于展示小数位的结论，布局规格不变。

### 修改与边界

- 全部价格展示统一取 `SymbolInfo.priceDecimals`：报价步长有效时从步长推导；步长缺失才使用 `pricePrecision`。目录完全缺失时保留按价格量级推导及极小价格不显示成零的后备规则。整数步长合法，显示零位小数。
- 接通头部价格 / 涨跌额、主图价格轴 / 最新价 / 十字线 / 均线、以价格为单位的副图、自选 / 板块表、画线标注与输入、复盘范围 / 回放及提醒通知。百分比、比值、成交量仍使用各自单位的格式。
- 切品种锁定元数据时同时保留 `tickSize` 与下单字段精度；回放先查记录所属品种的目录，缺目录才从该段历史价格推导，不能继承当前另一品种的精度。
- 调整旧的两位 BTC 断言，不保留旧展示口径。`FormatTests` / `HeaderStatsTests` 对固定输入验证闪迪 `1774.23` / `−19.73`、BTC 一位及极小价；真实行情验收只断言位数，不要求价格停在任务书示例值。
- 从 `71bd340` 建立 `/tmp/kanpan-p0-rework`，只复制这次返工的 31 个代码 / 测试文件，独立构建验证。逐文件 SHA-256 见 [source-manifest.json](P0/rework-1/logs/source-manifest.json)。P1 开头及旧 M8 日志不在此提交。

### 命令与结果

命令在上述独立工作树执行；完整输出在 [返工日志目录](P0/rework-1/logs/)。

| 命令 | 结果 |
| --- | --- |
| `xcodebuild build -workspace Kanpan.xcworkspace -scheme Kanpan -destination 'generic/platform=iOS Simulator' -derivedDataPath /tmp/kanpan-p0-rework-dd -jobs 2` | 退出 0，最终源码 BUILD SUCCEEDED |
| `swift test --package-path KanpanCore -j 2` | 361 条 Swift Testing、4 条 XCTest 通过 |
| `swift test --package-path KanpanNetwork` | 96 条通过 |
| `swift test --package-path KanpanData` | 184 条通过 |
| `make app-logic-test` | 八包 441 条通过：Symbols 158、Sector 35、Settings 124、Diagnostics 47、PrefsCodec 13、DeepLink 11、Scan 13、Alerts 40 |
| `cd KanpanChart && xcodebuild test -scheme KanpanChart -destination 'platform=iOS Simulator,id=42D33983-34FD-44C6-A635-ACDA22C7C0DA' -derivedDataPath /tmp/kanpan-p0-chart-dd -jobs 2 -test-timeouts-enabled YES -maximum-test-execution-time-allowance 180` | 126 条 Swift Testing、4 条 XCTest 通过；UIKit 包使用模拟器目标 |
| `cd Kanpan/KanpanTests && xcodebuild test -scheme KanpanMain -destination 'platform=iOS Simulator,id=42D33983-34FD-44C6-A635-ACDA22C7C0DA' -derivedDataPath /tmp/kanpan-p0-main-dd -jobs 2 -only-testing:KanpanMainTests/HeaderStatsTests -test-timeouts-enabled YES -maximum-test-execution-time-allowance 300` | 头部逻辑 15 条通过 |
| `cd Backend/kanpan-api && CARGO_TARGET_DIR=/Users/mdd/zhk/kanpan/Backend/kanpan-api/target cargo test --lib` | 153 条通过 |

```sh
ONLY_DEVICE='iPhone 16 Pro' \
ONLY_TESTING='KanpanUITests/MainScreenUITests/testHeaderStatsStayRightOfPrice,KanpanUITests/MainScreenUITests/testHeaderStatsAtLargestDynamicType,KanpanUITests/MainScreenUITests/testPricePrecisionInFavoritesAndSectorRows' \
DD=/tmp/kanpan-p0-rework-dd OUT=/tmp/kanpan-handoff-checks/p0-rework/final-16pro \
RES=/tmp/kanpan-handoff-checks/p0-rework/final-results-16pro bash Tools/ui-test.sh

# 上轮头部两条通过，列表用例的同名控件选择有歧义；按实际页面限定后只复跑该条。
ONLY_DEVICE='iPhone 16 Pro' \
ONLY_TESTING='KanpanUITests/MainScreenUITests/testPricePrecisionInFavoritesAndSectorRows' \
DD=/tmp/kanpan-p0-rework-dd OUT=/tmp/kanpan-handoff-checks/p0-rework/lists-16pro \
RES=/tmp/kanpan-handoff-checks/p0-rework/lists-results-16pro bash Tools/ui-test.sh

xcrun simctl spawn ACAF005F-5561-4C64-836B-45E4FB3C5C72 defaults write -g UIAccessibilityBoldTextEnabled -bool YES
# 读回 1，重启 app 后执行：
ONLY_DEVICE='iPhone 17e' \
ONLY_TESTING='KanpanUITests/MainScreenUITests/testHeaderStatsStayRightOfPrice,KanpanUITests/MainScreenUITests/testHeaderStatsAtLargestDynamicType' \
DD=/tmp/kanpan-p0-rework-dd OUT=/tmp/kanpan-handoff-checks/p0-rework/retry-17e-bold \
RES=/tmp/kanpan-handoff-checks/p0-rework/retry-results-17e-bold bash Tools/ui-test.sh
```

| 最终验证 | 结果 |
| --- | --- |
| iPhone 16 Pro 头部普通 / 最大字号 | 两条通过，42.014 秒 / 53.122 秒；四品种六格均 53pt、周期条起点 169.33pt |
| iPhone 16 Pro 自选 / 板块表 | 修正选择器后单条通过，30.047 秒；两张表四品种均为 2 / 2 / 8 / 1 位 |
| iPhone 17e 粗体普通 / 最大字号 | 两条通过，33.110 秒 / 40.907 秒；四品种六格均 53pt、周期条起点 154.33pt |

最大字号请求为 `UICTContentSizeCategoryAccessibilityXXXL`，头部仍封顶 **`.large`**。价格与涨跌额有位数断言；图表诊断断言当前品种及 `ChartState.decimals`，随后实际点出十字线并截图。自选 / 板块分别切到美股及加密分类，读取真实行情数字。

首次两机历史请求各遇过一次币安 429（等待窗口分别为 43 秒、22 秒）；头部报价已到，历史图没有就绪，相关断言如实失败。保留 [限流摘录](P0/rework-1/logs/history-rate-limit-excerpt.log) 和 [17e 初轮日志](P0/rework-1/logs/17e-first-history-limited.log)。用例增加限流后通过现有「重试」按钮重试一次，最终仍检查真实品种、精度与十字线；最终 17e 复跑未走重试分支。没有跳过断言、切行情线路或替换成假行情。测试结束已把该模拟器粗体设置恢复为 0。

### 返工前 / 后截图

| 品种 | 返工前（fd6a8f6，错误的五位） | 返工后（两位） |
| --- | --- | --- |
| 闪迪 | ![闪迪返工前](P0/screenshots/16pro-after-default-SNDKUSDT.png) | ![闪迪返工后](P0/rework-1/screenshots/16pro-normal-header-SNDKUSDT.png) |
| 美光 | ![美光返工前](P0/screenshots/16pro-after-default-MUUSDT.png) | ![美光返工后](P0/rework-1/screenshots/16pro-normal-header-MUUSDT.png) |

| 闪迪：390pt、粗体、最大字号 | AICoin 原始参照 |
| --- | --- |
| <img src="P0/rework-1/screenshots/17e-bold-max-header-SNDKUSDT.png" width="390"> | <img src="P0/screenshots/aicoin-reference.png" width="390"> |

左侧价格 + 下方涨跌小字、右侧数据格骨架一致。返工截图中闪迪最新价 `1768.00`、涨跌额 `−13.81`、均线 `1776.31` 均两位；美光 `1041.40`、均线 `1046.01` 均两位；实时价格随截图时刻变化。原图未改写数字、未裁边。

全部 36 张原始截图在 [screenshots](P0/rework-1/screenshots/)：两机普通 / 最大字号各有四品种头部与十字线截图，另有四张自选 / 板块表截图。重点证据：[美光最大字号](P0/rework-1/screenshots/17e-bold-max-header-MUUSDT.png)、[1000SATS 八位](P0/rework-1/screenshots/17e-bold-max-header-1000SATSUSDT.png)、[BTC 一位](P0/rework-1/screenshots/17e-bold-max-header-BTCUSDT.png)、[美光十字线](P0/rework-1/screenshots/16pro-normal-crosshair-MUUSDT.png)、[自选美股](P0/rework-1/screenshots/16pro-precision-favorites-stocks.png)、[自选加密](P0/rework-1/screenshots/16pro-precision-favorites-crypto.png)、[板块美股](P0/rework-1/screenshots/16pro-precision-sector-stocks.png)、[板块加密](P0/rework-1/screenshots/16pro-precision-sector-crypto.png)。

### 上游与后端只读验证

`GET https://fapi.binance.com/fapi/v1/exchangeInfo` 的原始精度字段摘录见 [exchange-precision.json](P0/rework-1/logs/exchange-precision.json)：SNDK / MU 的 `pricePrecision=5`、`tickSize=0.01000`，BTC 为 `2` / `0.10`，1000SATS 为 `8` / `0.00000001`。

`GET https://kanpan.107-174-172-10.sslip.io/v1/market/meta` 于 `2026-09-22T05:01:10Z` 返回 **200**，正文 44003 字节、顶层 `data`；输出见 [backend-readonly.json](P0/rework-1/logs/backend-readonly.json)。本次没有后端、数据库、网关修改，不涉及部署。全部验证在模拟器完成；未做真机验证、未寻找账号或密钥。
