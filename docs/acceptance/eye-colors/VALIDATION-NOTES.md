# 验收轮次与失败保留

最终候选以 `current-r8-*` 为准，不能累计旧轮次的通过数冒充最终结果。每个测试包的 summary 保留 passed/failed/skipped。

- `iphone15-first`：测试错误地把自选 key=value 诊断解析为 JSON，后改为正确协议。
- `iphone15-integrity-r1` / `current-r1`：发现品种行标识继承到收藏星标、键盘与系统筛选 Menu 布局冲突，以及清空旧序列后读取错误图区宽度。改为明确行按钮/星标、收键盘后独立筛选弹窗、保存实际图区宽度。
- `iphone15-focused-r2`：分类容器覆盖子按钮标识；切周期后的最终轴标签宽度仍改变根宽。补 contain 容器和按最终标签宽度解算视窗。
- `current-r3`：采样确认系统 Menu 在报价刷新时重复替换并动画，主动中止未完成的矩阵。
- `current-r4`：暴露透明分类按钮只命中文字的触区，以及测试 tearDown 读取被自选页覆盖的图表。补整块 contentShape；tearDown 只在已有失败时取证，图表不在界面则不强读 value。行拖动本身已完成，仍保留该轮失败，不把它计为最终通过。
- `current-r5`：预检发现测试文件比测试目标 object 新，中止并重新构建，不接受该轮结果。
- `current-r6`：配色/尺寸、行排序、四副图、边界与16次切周期报价检查通过；新增空文件夹的编辑断言错用了“全选”（空集合实际为“全不选”），改为在有品种的加密文件夹验证。独立采样显示仅 Equatable 隔离仍有 Menu 更新穿透，最终替换为 SwiftUI popover 列表。
- `current-r7`：popover 返回行情后测试过早点击过渡中的底栏，截图显示已返回图表；补等待页面实际可点击，不以重复点击掩盖失败。排序通过。
- `current-r8`：固定最终功能代码与构建产物，完整结果由 `current-r8-runs.jsonl` / `current-r8-final-summary.json` 给出。最多并行两台模拟器。新增功能无真机通过声明。

`/tmp` 原始 CPU sample 不提交完整大文件，保留选取的调用栈与采样环境/限制。原始 xcresult 及导出附件保留在本机、由 gitignore 排除；逐台 summary、执行日志、源/二进制哈希及代表截图入库。签名用的账号/配置不是本次交付内容。

- `iPad-A16-current-r8`：配色布局通过，长按排序失败。录像显示整行已抬起但未换位，不能据此认定空白区域不可长按；保留失败包与截图，使用最终构建继续隔离验收，不删除或计入通过数。

- `iPad-A16-final` / `iPad-Pro11-final`：最终小补丁构建各 2/2 通过。A16 同一坐标长按成功，不修改测试动作来绕过首次失败；首次未换位的原因仍未确定，追加三次同动作重复验收。矩阵以最终逐台记录汇总 13 台、31 项，原始失败保留。

- `iPad-A16-reorder-repeat`：最终构建同位置长按连续 3 次全部成功（执行日志记录 3 次，xcresult summary 将重复用例去重为 1 项）。首次未换位未能复现，仍保留为真机时需关注的间歇行为，不宣称已定位修复其根因。
- `iphone15-final`：最终源文件/二进制哈希对应构建，分类溢出与快捷收藏/市场板块 2/2 通过，0 跳过。
