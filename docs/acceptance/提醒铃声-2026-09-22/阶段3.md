# 提醒铃声 · 阶段 3

日期：2026-09-22。阶段开始 fetch/rebase 基线 `39523a8`。部署执行新规：实现先推 main，发布源码只从 origin/main 归档提取。

## 实现

- `push_alert` 接收 sound，生产请求与回归测试共用 `alert_payload` 构造函数。默认 / 清脆 / 电子 / 玻璃分别写入 `default` / `alert-crisp.caf` / `alert-electronic.caf` / `alert-glass.caf`。
- 触发时在提醒所有者的 `personal(user_id)` 事务中读取 `sync_objects` 的 `settings/chart`，绑定同一 user_id，排除 deleted；每次重新读取，不缓存到 Watch。该次推送的所有设备共用这一选择。
- 没有 settings 对象、缺少字段、类型错误、未知值或任意文件路径均回退 default。原有触发落库、同一条只触发一次、APNs 不配置时仍记录同步、死 token 清理均保留；没有新增迁移。

## 规定检查与模拟器

[最终检查](阶段3-logs/final-results.json)：通用模拟器构建、Core 361+4、Network 96、Data 184、Chart 126+4、app-logic 445、Rust lib **156** 全通过。新增 Rust 测试覆盖四档映射、旧/坏字段回退、payload sound 与深链保留。

首轮 Data 184 条中，既有 A-T05 在 `FeedBlockedRetryTests.swift:129` 的异步 banner 文案断言失败；未改行情源码或测试，同包原样重跑 184 条全通过。保存[首次日志](阶段3-logs/KanpanData.log)和[重跑日志](阶段3-logs/KanpanData-retry.log)，不把第一次写成全绿。

独立 iPhone 16 Pro · ringtone / iOS 26.5，`Tools/ui-test.sh` 仅执行通知相关的新用例，240 秒单例限时，**1 条通过**：正常画水平线 → 加入提醒 → 系统通知授权（明确断言允许按钮存在并点击）→ 默认试听 → 玻璃选择。未修改通知数据库、没有授权测试后门、没有凭证搜寻。[UI 汇总](阶段3-UI/summary.txt)，[系统授权原图](阶段3-截图/系统通知授权.png)，[授权后默认档](阶段3-截图/通知授权后-默认试听.png)。阶段二已验证四档与重启、三皮肤，不重复整个 UI 矩阵。

## 实际注入的 payload

以下 JSON 来自生产共用构造函数的实际测试输出（[构造日志](阶段3-logs/payload-builder.log)），原文件为[玻璃推送.apns](玻璃推送.apns)：

```json
{"aps":{"alert":{"body":"现价 64500","title":"铃声验收 · 玻璃"},"sound":"alert-glass.caf","thread-id":"alerts"},"link":"hkline://symbol/BTCUSDT"}
```

2026-09-22 15:29:54 CST，实际执行：

```sh
xcrun simctl push 42498667-73A3-4E02-83C2-7B73E957C514 com.mdd.kanpan docs/acceptance/提醒铃声-2026-09-22/玻璃推送.apns
```

退出 0，返回 `Notification sent to 'com.mdd.kanpan'`，系统实际呈现 Hkline「铃声验收 · 玻璃 / 现价 64500」横幅。[原始横幅截图](阶段3-截图/玻璃推送-系统横幅.png)，[发送记录](阶段3-logs/simctl-push.log)。CAF 已在包根目录且满足系统格式与时长要求。

这是**真实执行的模拟器推送注入**，通知文案为验收夹具；不冒充线上价格实际触发、Apple APNs 回执或真机听感。项目尚无 APNs 配置，按交接书不找密钥、不等待真机。

## 发布状态

本提交先交付已全绿的实现与模拟器证据，再执行部署。15:21 只读预检 96 份发布输入，有 8 份线上与 main 不同：[预检散列](阶段3-logs/source-preflight.json)。其中共享同步契约仍带第一线程先行部署、尚未合入 main 的 depth 与新指标词表；不能用本地工作树覆盖。新发布脚本只从已推送的 origin/main 取源码，覆盖前持锁重新核对散列；不属于 main 历史的线上源码会拒绝部署。

**此刻第三阶段尚未标记部署完成。** 发布结果、备份路径、重启时间、健康探测与二进制散列在后续本报告追加；不把代码推送等同部署。阶段四在发布验证完成后收口。
