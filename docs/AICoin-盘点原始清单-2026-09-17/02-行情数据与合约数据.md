# AICoin Android 资源盘点 —— 行情数据模块（合约数据 / 爆仓 / 资金 / 盘口 / 板块 / 其它）

资源根目录：`/Users/mdd/zhk/kanpan/refs/aicoin/res-full/`（apktool 解包，只读）。
文案取自 `values-zh-rCN/strings.xml`，尺寸取自 `values/dimens.xml`，颜色取自 `values/colors.xml`（夜间为 `_night` 后缀）。
布局中 `android:tag="skin:<res>:<attr>"` 为换肤标记，可直接对应日/夜两套色值。

范围内布局数量：`*_futures_*` 91、爆仓 (`liq|liqui|liquid|liquidation`) 35、`fund` 45、`miningdata|calendar` 28、`block` 29、`order` 91、`ui_quote_*` 3。
其中大量属于交易下单 / 用户资产 / VIP，统一归到末尾 D 类一行。

## 通用设计令牌（本模块反复出现）

| 令牌 | 值 |
|---|---|
| 文字 主/次/三级/提示 | `sh_base_text_primary #292d33` / `secondary #525a66` / `tertiary #7a8899` / `hint #c7c7c7`（列表副标题 `ui_ticker_list_item_subtitle_text_color #8a97a0`） |
| 高亮色 | `sh_base_highlight_color #1478fa` |
| 页面底 / 分割 | `sh_base_bg_color #f7f9ff`，页面灰 `#f7f8fa`，分割线 `1px`，分割色 `sh_base_divider_fill_color`，边框填充 `#f2f3f5` |
| 涨/跌（爆仓多空条） | 多 `#32a853`，空 `#eb4236` |
| 列表行 | `ui_ticker_list_item_block_height 65dp`；列表标题行 `36dp`；第三列宽 `120dp`（短版 79dp）；首列左距 15dp；名称 `15sp`、副标题 `11sp`、主价 `18sp`、主价左距 `12sp`；涨幅徽章 `80×30dp 16sp`，右距 15dp |
| 样式 | `ui_ticker_AppSkin.ListContent.TitleText` = 15sp `#292d33` 单行省略；`.SubtitleText` = 11sp `#8a97a0`；`ui_ticker_AppSkin.ListTitle.TitleText(.Sort)` = 列表表头字 + 排序箭头 `ui_ticker_list_title_ic_sort_none`；`ui_ticker.LiqTitle` = 15sp 粗 `#292d33`；`ui_ticker.LiqRadioButton` = 13sp、minWidth 41dp、选择器 `ui_ticker_liq_tab_text_selector`；`ui_ticker.LiqListTagText` = 12sp `#7a8899`；`ui_home.FuturesData.ItemPrimaryText` = 12dp `#292d33` mt5；`.ItemSecText` = 12dp `#7a8899` mt6 |
| 图表控件 | `app.aicoin.ui.ticker.widget.TrendView`（迷你折线）、`LiqDayGraph`（日柱）、`LiqNumberGraph`（人数柱/折线）、`LiqRadioBar`（多空比例条）、`app.aicoin.common.widget.PositionPercentBar`（多空占比条）、`app.aicoin.ui.ticker.widget.HistoView`（盘口深度背景条）、`m.aicoin.ticker.fund.widget.IndicatorView/ChartLine/ChartHistogram/IndexPieView`（基本面页） |

Kanpan 现状（用于 A/B 判定）：iOS SwiftUI，仅有 Binance USDT 永续 `ticker/24hr`、K 线、以及直连 `/futures/data/openInterestHist`（网关线路走 OKX，无法代理 `/futures/data/*`）；没有 depth、aggTrades、fundingRate/premiumIndex、forceOrder、多空比、basis。

---

## 模块 1：合约数据（持仓量 / 资金费率 / 基差 / 多空比 / 列表）

### 1.1 首页「BTC合约」概览卡

- 布局文件：`ui_home_frg_futures_data.xml`、`ui_home_frg_part_futures_data.xml`（"市场热度" + `rv_hot_list`）
- 元素与尺寸：
  - 标题 `BTC合约` 15sp 粗 `#292d33`，行高 38dp，左距 15dp；右侧 `统计数据:%s` 11sp `#7a8899`
  - 两张并排卡（padding 8dp，外距 15/4/4/15dp）：
    - 左卡：`24H最大爆仓单(%s)` 标签 → 值 11sp；`1H内总爆仓`、`24H总爆仓`（ItemPrimaryText 12dp `#292d33` / ItemSecText 12dp `#7a8899`）
    - 右卡：`当前持仓量(%s)` → 值 11sp；`24H最高持仓`、`24H最低持仓`、`多空持仓人数比`
  - 底部 `PositionPercentBar` 高 20dp，mt10，左右 15dp，两端文案 `(多单)` / `(空单)`
  - 「市场热度」块：标题 15sp 粗 + 周期标签 11sp + `rv_hot_list`（item 布局未在 `*_futures_*` 前缀内，需另查）
- 文案：`ui_home_futures_*`：`BTC合约`、`统计数据:%s`、`24H最大爆仓单(%s)`、`1H内总爆仓`、`24H总爆仓`、`当前持仓量(%s)`、`24H最高持仓`、`24H最低持仓`、`多空持仓人数比`、`(多单)`、`(空单)`
- 图表类型：水平双色占比条（PositionPercentBar）
- 数据需求：
  - 当前/24H 最高/最低持仓：Binance `GET /futures/data/openInterestHist?symbol=BTCUSDT&period=5m&limit=288` → `sumOpenInterest`、`sumOpenInterestValue`；OKX `GET /api/v5/public/open-interest?instId=BTC-USDT-SWAP` → `oi`、`oiCcy`
  - 多空持仓人数比：Binance `GET /futures/data/globalLongShortAccountRatio?symbol=BTCUSDT&period=5m` → `longAccount`、`shortAccount`、`longShortRatio`；OKX `GET /api/v5/rubik/stat/contracts/long-short-account-ratio?ccy=BTC`
  - 1H/24H 总爆仓、24H 最大爆仓单：仅 Binance WS `!forceOrder@arr`（`o.S/o.q/o.ap`）自行累计；OKX `GET /api/v5/public/liquidation-orders?instType=SWAP&uly=BTC-USDT&state=filled`。AICoin 的「全网」口径为多交易所聚合 → 无公开数据源
- 分类：**B**
- 备注：持仓量部分 Kanpan 已具备（直连），缺爆仓与多空比；「市场热度」= AICoin 关注度，归 D。

### 1.2 持仓量榜（含 7 日持仓迷你图）

- 布局文件：`ui_ticker_frg_futures_interest.xml`、行 `ui_ticker_item_futures_interest_list_recycler_content.xml`；横屏入口 Activity `CoinFuturesListLandActivity` / `MarketFuturesListLandActivity`
- 元素与尺寸：
  - 表头 36dp：`筛选` 12sp `#7a8899` + 筛选图标；中列排序文案（`ListTitle.TitleText.Sort`）；右列 `7日持仓` 宽 101dp；1px 分割
  - 行 65dp：`title`(15sp) + `title_pair`(11sp，左距 4dp) / `subtitle`(11sp `#8a97a0`)；持仓量 `vol` 18sp 左距 12sp + `vol_coin` 小字；右列 120dp 内 `TrendView` 宽 79dp（迷你折线）
- 文案：`ui_ticker_list_title_label_7d_position_trend = 7日持仓`、`ui_ticker_futures_fee_filter = 筛选`、`ui_ticker_futures_coins_title = 选择项目`、`ui_ticker_futures_coins_choice_title = 支持多选`
- 图表类型：迷你折线（sparkline，7 日）
- 数据需求：Binance `GET /futures/data/openInterestHist?symbol=…&period=1d&limit=7`（或 `1h`×168）→ `sumOpenInterest`、`sumOpenInterestValue`、`timestamp`；实时值 `GET /fapi/v1/openInterest`。OKX `open-interest` 仅当前值，历史用 `/api/v5/rubik/stat/contracts/open-interest-volume?ccy=BTC&period=1D`
- 分类：**A**（Binance 单交易所口径）
- 备注：Kanpan 已有 openInterestHist 直连；网关线路不能代理 `/futures/data/*`，网关模式需降级为 OKX rubik 或不显示 7 日图。多交易所横向对比 → 无公开数据源。

### 1.3 资金费率榜

- 布局文件：`ui_ticker_frg_futures_fee.xml`、行 `ui_ticker_item_futures_fund_list_recycler_content.xml`
- 元素与尺寸：表头 36dp 同 1.2，中列 `资金费率`，右列（累计）101dp；行 65dp：`title`/`title_pair`、`subtitle`/`subtitle_pair`，`trade_24h` 18sp（当期费率），右列 120dp `net_inflow_24h` 18sp（7 日累加）
- 文案：`ui_ticker_futures_fee_*`：`资金费率`、`距结算 %s`、`%s 永续`、`筛选`/`全部`/`支持多选`、`选择平台`、`选择项目`、`费率显示`（`百分比 %`/`千分比 ‰`/`万分比 ‱`）、`统计类型`（默认 `7日累加`）、`无惧市场涨跌，2步轻松永续套利`（教程入口，可忽略）
- 图表类型：无（纯列表）
- 数据需求：
  - 当期/预测费率与结算倒计时：Binance `GET /fapi/v1/premiumIndex`（全量）→ `lastFundingRate`、`nextFundingTime`、`markPrice`、`indexPrice`；OKX `GET /api/v5/public/funding-rate?instId=…` → `fundingRate`、`nextFundingRate`、`fundingTime`
  - 7 日累加：Binance `GET /fapi/v1/fundingRate?symbol=…&limit=21`（8h×21）求和；OKX `GET /api/v5/public/funding-rate-history?instId=…&limit=21`
- 分类：**B**
- 备注：Kanpan 缺 premiumIndex/fundingRate；两个接口都是 `/fapi/v1/*`，网关（OKX）侧可用 OKX 同类接口替代。

### 1.4 价差率 / 基差

- 布局文件：`ui_ticker_frg_futures_diff_list.xml`、表头 `ui_ticker_item_futures_diff_list_recycler_title.xml`、行 `ui_ticker_item_futures_diff_list_recycler_content.xml`、分组头 `ui_ticker_item_futures_diff_titile.xml`
- 元素与尺寸：
  - 表头 36dp：筛选图标 + `筛选`、中列文案、右列 120dp `价差率`
  - 行 65dp：预警图标(gone)、`title` 15sp、减号图标、`subtitle` 11sp `#7a8899`、`main_price` 18sp、`sub_price`、右侧 `diff_rate` 徽章 80×30dp 16sp（涨跌底色），右距 15dp
  - 分组头 44dp：展开图标 16dp + 组名 14sp `#1478fa` + 右侧最新价
- 文案：`ui_ticker_futures_compare_type_title = 选择基底`、`ui_ticker_futures_compare_choice_title = 仅支持选择1个`、`价差率`
- 图表类型：无
- 数据需求：Binance `GET /fapi/v1/premiumIndex` → `(markPrice-indexPrice)/indexPrice`；历史 `GET /futures/data/basis?pair=BTCUSDT&contractType=PERPETUAL&period=5m` → `basis`、`basisRate`、`futuresPrice`、`indexPrice`；OKX `GET /api/v5/rubik/stat/contracts/…` 无基差，可用 `mark-price` 与 `index-tickers` 自算。
- 分类：**B**
- 备注：AICoin 的「选择基底」可跨平台（如 Binance 永续 vs OKX 现货）→ 该口径无公开聚合源，Kanpan 只能做「同一交易所 合约-指数」基差。

### 1.5 热门合约（1H 关注度）

- 布局：`ui_ticker_frg_futures_hot.xml`、行 `ui_ticker_item_futures_hot_rank_content.xml`（65dp：名次图标 22dp / 14sp 粗；名次变化图标 + 9sp；`text_title` 左距 45dp、pair、subtitle；`main_price` 18sp、`sub_price`；`text_attention` 18sp 宽 98dp；表头 `排名`/`名称`/`1H关注度`(98dp)）
- 分类：**D**（关注度为 AICoin 私有指标，无公开数据源）

### 1.6 合约异动

- 布局：`ui_ticker_frg_change_futures.xml`（异动筛选 part + AppBar 头 + recycler）；文案 `ui_ticker_futures_changes_tab_hint = 合约行情异动，实时更新`
- 数据需求：可由 Kanpan 已有 ticker24h/K 线自算（5m 涨幅、量比、振幅等，文案见 `ui_ticker_label_growth_rate_5m`、`ui_ticker_label_amplitude_5m`）。
- 分类：**C**
- 备注：行布局与异动类型枚举（放量、急涨、急跌等）没有落在 `*_futures_*` 前缀，需要真机镜像确认行内元素与异动种类。

### 1.7 合约列表（交割/永续通用列表）

- 布局：`ui_ticker_frg_futures_fix_list.xml` + 表头（搜索框 29dp，hint `搜索 项目简称/全称/英文名称/排名`；`名称`/`流通市值` + 横屏图标）；行复用 `ui_ticker_item_price_list_recycler_content`（属行情列表模块）
- 数据需求：Binance `GET /fapi/v1/exchangeInfo`（`contractType`、`deliveryDate`）+ `ticker/24hr`
- 分类：**A**（Kanpan 已有 ticker24h；交割合约仅需 exchangeInfo 过滤）

### 1.8 多空比（占比条组件）

- 布局：`PositionPercentBar` 在 `ui_home_frg_futures_data.xml`（20dp 高）与 `ui_ticker_block_detail_list.xml`（145×22dp，配 `普涨/普跌/平稳` 标签 11sp）复用；文案 `多空持仓人数比`、`(多单)`/`(空单)`、`ui_ticker_liquid_bid = 多头`、`ui_ticker_liquid_ask = 空头`
- 数据需求：Binance `globalLongShortAccountRatio`、`topLongShortAccountRatio`、`topLongShortPositionRatio`、`takerlongshortRatio`（均 `/futures/data/*`，字段 `longShortRatio`/`longAccount`/`shortAccount`、`buySellRatio`/`buyVol`/`sellVol`）；OKX `/api/v5/rubik/stat/contracts/long-short-account-ratio`、`taker-volume`
- 分类：**B**
- 备注：Binance 端只能直连；网关线路需走 OKX rubik。

### 1.9 合约详情页扩展字段（交叉引用）

- 布局：`ui_ticker_include_detail_price_block.xml`（`净流入`、`高`、`低`、`FR`、`仓`、`额`、`量`、`涨幅`，AutoSizeTextView）、`ui_ticker_include_detail_price_extra_block.xml`（`预测费率`、`基差率`、`开`、`收`、`流通市值`、`量比`、`委比`、`市盈率`、`涨速`、`换手率`、`5m振幅`）、`ui_ticker_detail_block_tip_dialog.xml`（名词解释：`当前交易对资金费率:`、`当前交易对预测费率:`、`我已了解`）
- 数据需求：`FR`/`预测费率`/`基差率` ← `premiumIndex`；`仓` ← `/fapi/v1/openInterest`；`委比` ← `depth`；`净流入` 见 3.1
- 分类：**B**（视觉细节归行情详情模块盘点）

---

## 模块 2：爆仓

页面骨架：`ui_ticker_act_liquid_summary.xml` / `ui_ticker_act_liquid_rank.xml`（返回 + MagicIndicator + ViewPager；Activity `LiquidSummaryActivity`、`LiquidRankActivity`），`ui_ticker_frg_futures_liqui.xml`（纵向滚动，summary / ladder / platform / 7d / recent 五个容器，间距 8dp）。

### 2.1 爆仓总览（BTC 集中区间 + 统计网格）

- 布局：`ui_ticker_part_futures_liqui_summary.xml`、`ui_ticker_frg_liq_stat_all.xml`、格子 `ui_ticker_item_liq_summary_grid.xml` / `ui_ticker_item_liq_summary.xml` / `ui_ticker_item_liq_stat_all.xml`
- 元素与尺寸：
  - 外层 padding 15dp；标题行 13sp `#525a66`：`BTC 爆仓价格集中区间: $%s ~ $%s`（HTML 粗体版 `ui_ticker_futures_liq_summary_title_format`）
  - 第二行：`<b>BTC</b> 爆仓 %s笔，<b>%s时</b> 爆仓最集中`；卡片版第三行 `<b>%s</b> 爆仓 %s，占全网 <b>%s</b>`
  - 卡片（`ui_base_important_bg`，外距 15dp，内距 16/10dp）标题 16sp 粗
  - 网格 `rv_grid` mt14：格子 = 副标签 11sp `#525a66` + 价格 12sp `#292d33` + 标签 12–13sp 粗 + 百分比 10sp 粗；标签集合 `1H爆仓`、`24H爆仓`、`多单爆仓`、`空单爆仓`、`最大爆仓`、`BTC每笔均爆`
  - 提示 `* 统计周期:24H` 11sp；`更新于 %s`
  - 快捷条 48dp，底 `#0d1478fa`（夜 `#1a105fc7`），三个按钮 13sp：`爆仓人数` / `项目爆仓` / `价格分布`，竖分割 1×24dp `#1a1478fa`
- 图表类型：无（数值网格）
- 数据需求：Binance WS `wss://fstream.binance.com/ws/!forceOrder@arr`（`o.s` 交易对、`o.S` 方向、`o.q` 量、`o.ap` 均价、`o.T` 时间）自建 1H/24H 滚动累计；OKX `GET /api/v5/public/liquidation-orders?instType=SWAP&uly=BTC-USDT&state=filled&limit=100`（`details[].bkPx/sz/side/ts`）。「占全网」「集中区间」「最集中时段」为 AICoin 多交易所聚合 → 无公开数据源
- 分类：**B**
- 备注：Binance 已下线公开 REST `allForceOrders`，`GET /fapi/v1/forceOrders` 需签名且只返回本账户；因此历史需 Kanpan 后端持续订阅 WS 落库。

### 2.2 7 日爆仓（日柱图）

- 布局：`ui_ticker_part_liq_day_summary.xml` + 弹窗 `ui_ticker_popup_liq_graph.xml`
- 元素：标题 `7日爆仓` 15sp 粗 + 单位 12sp 粗；图例 `多单(%s)`/`空单(%s)` 高 38dp；`LiqDayGraph` 高 254dp；下方 `rv_liq_coin`；弹窗白字 12sp（`总爆仓/多单/空单`，padding 10dp，底 `#cc333333`）；坐标线色 `#f0f4ff`、选中柱 `sh_base_view_bg_pressed`
- 图表类型：分组/堆叠日柱（多空双色）
- 数据需求：同 2.1，需按日聚合 7 天；无公开历史接口，必须自建累计
- 分类：**B**

### 2.3 平台爆仓（多交易所占比）

- 布局：`ui_ticker_part_liq_plat_summary.xml`（标题 `平台爆仓` + 单选 `1H/4H/24H` 38dp）、行 `ui_ticker_item_liq_plat_summary.xml` 44dp（图标 18dp、平台名 14sp 66dp、金额 10sp 100dp、`LiqRadioBar` 12dp、百分比 11sp 60dp）；提示 `注：币安平台仅支持部分爆仓数据的统计` 11sp `#b7bfc8` 38dp
- 分类：**D**（多交易所聚合，无公开数据源）

### 2.4 大额爆仓（最近爆仓流）

- 布局：`ui_ticker_part_liq_recent.xml`（标题 `大额爆仓` + 单选 BTC/ETH/EOS + 箭头）、行 `ui_ticker_item_recent_liq.xml`
- 行 61dp：平台 logo 18dp + 平台名 14sp + 时间 11sp（列宽 90dp）；最新价 13sp + 类型 11sp 粗（多/空）；交易对 13sp + 摘要 11sp；总额 10sp + 金额 11sp
- 文案：`大额爆仓`、`多单`/`空单`、`更新于 %s`
- 图表类型：无
- 数据需求：Binance WS `!forceOrder@arr`（单条即一行）；OKX `liquidation-orders`（最近 100 条）
- 分类：**B**

### 2.5 爆仓排行（24H / 3 日）

- 布局：`ui_ticker_part_liq_rank_list.xml`、`ui_ticker_frg_liq_rank_list_cycle.xml`（单选 `24H`/`3日`）、表头 36dp 权重 `#`0.07 / `类型`0.1 / `合约`0.3 / `破产价(%s)`0.2 / `爆仓额(%s)`0.2；行 `ui_ticker_item_liq_rank.xml` 44dp（名次 13sp `#8a97a0` 或 17dp 奖牌图标、方向 14sp 粗、logo 18dp + 名称 14sp 自动缩 11–14、价格 13sp、金额 10sp）；`ui_ticker_frg_liq_big.xml` 为 tab + 子列表
- 文案：`爆仓排行`、`破产价(%s)`、`爆仓额(%s)`、`($)`/`(¥)`、`3日`
- 数据需求：同 2.1 自建累计后按单笔金额排序（`o.q*o.ap`）
- 分类：**B**

### 2.6 项目爆仓（按币种展开）

- 布局：`ui_ticker_frg_liquid_coin.xml`（tab + 可展开 rv）、体 `ui_ticker_item_liq_coin_body.xml` 111dp（币种 13sp 粗、价格 12sp、金额 11sp、百分比芯片 72×22dp `#1478fa` 底 `#80f7f8fa`）、筛选芯片 24dp 13sp、组标题 44dp
- 文案：`项目爆仓`、`占%s`、`全网`、`历史`
- 数据需求：同 2.1 按 `o.s` 聚合；「占全网」无公开数据源
- 分类：**B**

### 2.7 爆仓人数

- 布局：`ui_ticker_frg_liquid_people.xml`（`24H/48H/3D/7D` 四段，各 标题 44dp + 图例 27dp `人数`/`多头`/`空头` 12sp + `LiqNumberGraph` 260dp）、弹窗 `ui_ticker_popup_liq_number.xml`（`总人数/多头/空头`）；文案 `过去 … ，全网总爆仓人数 … 居多`、`<b>%s</b> 爆仓总额 <b>%s</b>%s, <b>%s</b> 占比 <b>%s</b>`
- 分类：**D**（"人数"口径任何公开接口都不提供；forceOrder 只能数"笔"）

### 2.8 价格分布 / 预估清算图

- 布局：`ui_ticker_frg_liquidation.xml` = `m.aicoin.base.web.common.NestedDWebView`（无原生视图）；文案 `价格分布`、`当前交易对不支持预估清算图`、`当前为现货交易对，请选择合约交易对`
- 分类：**D**（WebView + AICoin 私有估算，无公开数据源）

### 2.9 机器人消息内的爆仓表

- 布局：`view_page_item_futures.xml`、`item_liq_futures.xml`、`item_liq_market.xml`、`item_liq_coin.xml`、`incoming_liq_condition_message.xml`（表头 29dp 12sp `类型`40dp/`合约`/`破产价 ($)`80dp/`爆仓额 ($)`70dp；行 45dp 13sp；多空条 80×15dp `#32a853`/`#eb4236`；文案 `以下是爆仓情况总览`、`合约`/`平台`/`项目`、`更多详情`）
- 分类：**D**（AI 对话消息卡片）

---

## 模块 3：资金流向

### 3.1 首页「资金流向」榜（竖屏 + 横屏）

- 布局：`ui_home_act_home_fund.xml`（标题栏 44dp `资金流向` + 刷新图标 46dp；周期 tab `MagicIndicator` 40dp，阴影 `#32000000`；`ui_home_item_ticker_sort_screen_title` 排序头；`pager_rank`），行 `ui_home_item_fund_flow_content.xml`（65dp：预警图标(gone)、`title`15sp/`title_pair`、`subtitle`/`subtitle_pair` 11sp、`column` 18sp、右列 120dp 内 `last_column` 80×30dp 18sp 徽章，底部 1px 分割左右 12dp）、简版 `ui_home_item_fund_flow.xml`（68dp：名称 13sp `#7a8899`、pair 13sp、平台 13sp、值 18sp 粗）
- 横屏：`ui_home_land_act_home_fund.xml`（`LockColumnHorizontalScrollView` 锁首列）、页头 `ui_home_land_include_fund_list_page_title.xml`（44dp，标题 13sp `#37426b`、搜索框 325×29dp、`24H:` `最大成交额` `最大净流入`）、表头 `ui_home_land_include_fund_list_recycler_title.xml`（30dp；首列 133.5dp，`最新价` 90.5dp，`成交额`/`成交额占比`/`净流入`/`流入`/`流出` 各 106dp，均可排序）、行 `ui_home_land_item_fund_list_recycler_content.xml`（50dp，名称 + pair 4dp、主价/副价 1dp 间距，各列右对齐）；Activity `HomeFundActivity` / `HomeFundLandActivity`
- 文案：`资金流向`、`最新价`、`成交额`、`成交额占比`、`净流入`、`流入`、`流出`、`最大成交额`、`最大净流入`、`24H净流入`、`7日净流入`、`主力净流入`、`24H主力净流入`、`净流入涨跌`
- 图表类型：无
- 数据需求：AICoin 的流入/流出为按主动买卖方向累计的多交易所成交额。单交易所近似：Binance `GET /fapi/v1/aggTrades`（`m` = 是否买方为 maker → 主动买/卖）或 WS `<symbol>@aggTrade` 自累计；粗粒度可用 `GET /futures/data/takerlongshortRatio?symbol=…&period=5m` → `buyVol`、`sellVol`（`/futures/data/*` 直连限定）；OKX `GET /api/v5/rubik/stat/taker-volume?ccy=BTC&instType=CONTRACTS` → `buyVol`、`sellVol`
- 分类：**B**
- 备注：口径不等价（AICoin 为多所聚合且含「主力」阈值过滤），只能做 Binance 单所版。

### 3.2 自选页「资金」列

- 布局：`ui_optional_item_fund_list_recycler_content.xml`（65dp，`net_inflow_24h` 18sp + 右列 `net_inflow_7d` 120dp 18sp）；`ui_optional_frg_fund.xml` 为占位（纯色 `#ff8373`）
- 数据需求：同 3.1
- 分类：**B**

### 3.3 币种基本面 / 资金页（act_fund 系列）

- 布局：`act_fund.xml`、`frg_fund.xml`（币 logo 29dp + 名称 15sp 粗 + 排名徽章 10sp 白字 `#1478fa`；`市值`/`占比` 11sp；主价 26sp `#323232` + 符号 18sp；`IndicatorView` 120×82dp；`所属板块：` 13sp + rv；`*全部信息数据仅供参考，不作为任何投资建议！` 10sp 粗 `#eb4236`；`专业K线` 按钮 27dp 圆角 3dp；底部广告条 46dp `前往` 63×30dp）、`fund_frg_fund.xml`（`主力买卖成本 >`、`ChartSmoothCurve`、`PageDWebView`）、`fund_frg_analysis.xml`（`基本信息`：发行时间/发行价格/发行总量/流通量/核心算法/激励机制/所属地区/简介；`统计数据` `AnalysisStatisticsView`；`代码活跃度`/`社区规模` `ChartLine`）、`fund_frg_market*.xml`（`上线平台`/`上线前10平台`/`下架平台`/`下架前10平台`）、`fund_frg_partition.xml`（`项目对24H成交额占比`/`成交量占比`/`成交单数占比` `IndexPieView`、`主力成交差 >`）、`fund_part_analysis_info.xml`（`市值`/`全球：`/`占总市值：`/`流通`/`总量：`/`流通率：`/`成交`/`24H量：`/`24H额：`/`换手率：`）、`fund_frg_safety.xml`、`fund_item_analysis_holder_address.xml`、`fund_act_team.xml`、`fund_frg_introduction.xml`（WebView）、`act_temp_fund*.xml`（`简介`/`统计`/`市场` 分段 + WebView）、`ui_ticker_fund_partition_*`（`成交市场`/`今日关注度`/`已下架`）、`ui_ticker_frg_brief_detail_fund.xml` / `ui_ticker_frg_share_fund.xml`（`RingCircleView` 平台占比环图）
- 分类：**D**（项目基本面、链上持币、GitHub/社区、跨平台成交占比，均无 Binance/OKX 公开源）

---

## 模块 4：盘口与成交（`*_order_*` 中的公开数据部分）

### 4.1 买卖盘（深度列表）

- 布局：`frg_ticker_orderbook.xml`、行 `item_ticker_orderbook.xml` / `ui_ticker_item_ticker_orderbook.xml`；设置页 `act_ticker_ordercount.xml`（`委单设置`、`委单条数过多会消耗大量流量，请酌情选择`）
- 元素与尺寸：表头 30dp，左右 padding 4dp，中缝 1px `#eaeaea`；四列 `数量`(可排序) `买一价` | `数量`(可排序) `卖一价`，`AppSkin.TickerAnalyse.ListTitle` 13sp `#7a8899` 自动缩 9–13sp，左右 padding 12dp；行 35dp：左半 `HistoView`(histoDirection=right) 背景条 + `text_bid_amount`/`text_bid_price`，右半 `HistoView`(left) + `text_ask_amount`/`text_ask_price`，文字 `AppSkin.TickerAnalyse.ItemContent` 12sp `#7a8899`
- 文案：`ui_ticker_analyse_title_*`：`数量`、`数量(%s)`、`价格(%s)`、`买一价`、`卖一价`、`委比`、`委差`、`以上数据与信息仅供参考，不构成投资建议`
- 图表类型：行内深度背景条（HistoView）
- 数据需求：Binance `GET /fapi/v1/depth?symbol=…&limit=20|50|100` → `bids[][price,qty]`、`asks[]`；WS `<symbol>@depth20@100ms`；OKX `GET /api/v5/market/books?instId=…&sz=20`
- 分类：**B**

### 4.2 成交明细

- 布局：`frg_ticker_dealamount.xml`、行 `item_ticker_dealamount.xml`
- 元素与尺寸：表头 30dp mt8：`成交时间`(weight 4) `成交价`(5) `成交量`(5，可排序，自动缩 9–13sp) + 空列(4)；行 35dp 同权重，`HistoView` 在最右列（`#fbe2e0` 红底，方向 left）；字 12sp `#7a8899`
- 文案：`成交时间`、`成交价`、`成交价(%s)`、`成交量`、`成交量(%s)`
- 图表类型：行内量能背景条
- 数据需求：Binance `GET /fapi/v1/aggTrades?symbol=…&limit=100` → `p`、`q`、`T`、`m`；WS `<symbol>@aggTrade`；OKX `GET /api/v5/market/trades?instId=…&limit=100`
- 分类：**B**

### 4.3 大单 / 主力 / 跨平台大单 / 大单预警

- 布局：`ui_kline_frg_order_and_trade.xml`（`当前为24小时前大单数据`）、`ui_kline_part_large_order_info.xml`（`委单价格`/`初始`/`当前`/`成交`/`成交比例`/`最大单笔`/`成交笔数`）、`ui_kline_frg_order_point_info.xml` + `ui_kline_item_kline_order_info.xml`（`委托类型/数量/价格/时间`，属交易记录）、`ui_kline_order_show_menu_layout.xml`（`下单显示`、`买卖记录`、`持仓成本线`、`预估强平线`）、`frg_indicator_filter_large_order.xml`（`跨平台大单`、`(最多可选4个平台)`、`现货`/`永续合约`/`交割合约`、`挂单时长`、`大单金额(万美元)`、`委单金额`、`成交金额`）、`ui_vip_frg_large_order_privilege.xml` / `ui_vip_item_large_order_card_content.xml` / `ui_vip_frg_big_deal.xml` / `ui_vip_item_big_deal_card_content.xml`（`挂单差`、`实际成交差`、`买单挂单金额 $`、`解锁方向`、`下单`、`预警`、`详情`）、`ui_alert_dialog_alert_big_order.xml`（大单预警条件）
- 分类：**D**（VIP 私有大单识别 + 交易记录 + 预警模块）

### 4.4 交易盘口（下单页）

- 布局：`part_futures_order_book.xml`、`item_futures_order_book.xml`、`item_portrait_order_book.xml`（`买卖盘`、`价格(%s)`、`数量(%s)`）
- 分类：**D**（下单页组件）

---

## 模块 5：板块

### 5.1 板块列表

- 布局：`ui_ticker_frg_blocks_list.xml`（SwipeRefresh + 表头 `ui_ticker_item_blocks_list_recycler_title.xml` 36dp：`板块｜总市值` / 中列排序 / 右列 115dp `1H关注度` 或 `排名` + 1px 分割 + `coin_cap_list`）、行 `ui_ticker_item_block.xml`
- 行元素：左右 padding 15dp，内 padding 上 18dp 下 15dp；`tv_item_block_name` 15sp + 标签 `iv_item_block_tag` 11sp（padding 4/2dp）+ `tv_item_block_value` 11sp `#8a97a0` mt7；中间涨跌徽章 `tv_item_block_price` 76×30dp 15sp（`ui_ticker_item_block_pct_green_bg`）；右侧 116dp 区域 paddingStart 45dp，两行领涨币 `first/second_coin_name` 14sp 宽 60dp，间距 6dp；底 1px 分割
- 文案：`ui_ticker_blocks_lable_name = 板块｜总市值`、`所属板块：`、`1H关注度`、`排名`
- 图表类型：无
- 数据需求：成分币 24h 涨跌 ← Kanpan 已有 `ticker/24hr`；板块归属映射需自建（无 Binance/OKX 接口）；总市值需第三方（非本次接口清单）；`1H关注度` 无公开源
- 分类：**B**

### 5.2 板块详情

- 布局：`ui_ticker_block_detail_list.xml`（Activity `BlockDetailActivity`）、弹窗行 `ui_ticker_block_detail_dialog_list_item.xml`（`——— 已加载全部内容 ———`）、标签 `ui_ticker_block_detail_tag_item.xml`
- 元素：顶部 paddingTop 13dp，返回 + 标题 18sp + 标签 11sp；价格块（左右 15dp，mt15）：`tv_block_price` 19sp 粗 + `市值：-` 11sp `#8a97a0` mt8；右侧 `普涨/普跌/平稳` 标签 11sp（padding 7/4dp）+ `PositionPercentBar` 145×22dp；简介卡 `ui_ticker_block_dialog_text_bg`（内距 9dp，提示 14sp `#faad14`，正文 14sp `#7a8899` 最多 6 行，展开箭头）；成分列表：表头 36dp（`项目 | 总市值`、`价格`、`成交额`、`($)`/`(¥)`）+ 1px + rv + 回顶浮钮
- 数据需求：同 5.1（上涨/下跌家数比自 ticker24h 统计）
- 分类：**B**

### 5.3 币址查询 / 我的币址

- 布局：`act_block.xml`（`币址查询`/`我的币址`）、`act_block_custom_add.xml`（`添加币址`、`请输入币址名称`、`请输入比特币地址`）、`frg_block_query.xml`、`footer_blockquery.xml`（`数据来源:block.okcoin.cn`）、`item_block_detail.xml`、`item_blockquery.xml`
- 分类：**D**（链上地址查询，非行情）

---

## 模块 6：挖矿数据

- 布局：`act_miningdata.xml`（tab `比特币`/`莱特币`；Activity `MiningDataActivity`）、`frg_miningdata.xml`（ListView）、`item_miningdata.xml`（左 `item_type` 14sp `#323232` 上下 12dp、右 `item_value`）、`item_miningdata_two_row.xml`（右侧两行，第二行 11sp `#7a8899`）；文案 `请输入您的算力`；另有 `CoinMiningListLandActivity`/`MarketMiningListLandActivity`、`ui_ticker_label_mining_pct = 挖矿比例`
- 分类：**D**（难度/算力/收益估算，无 Binance/OKX 源）

## 模块 7：事件日历

- 布局：`act_calendar.xml`（`事件日历`、`今`、`只看重要`，`PartialSwipeCalendarLayout`/`CustomMonthView`）、`act_calendar_detail.xml`、`act_calendar_record.xml`、`item_calendar_event.xml`（`待定`）、`item_calendar_date_header.xml`、`ui_news_dialog_calendar_reminder.xml`（`设置提醒`、`PC通知`/`APP通知`/`邮件通知`/`同步到手机日历`、`将在 %s 提醒您`）、`frg_calendar_share_poster.xml`（分享海报）、`ui_kline_item_calendar_day.xml`
- 分类：**D**（资讯/日历内容）

## 模块 8：近期上新

- 布局：`ui_home_act_latest_update.xml`（Activity `LatestUpdateActivity`）
- 元素：标题栏 44dp `近期上新`；两张 100dp 卡（padding 8dp，间距 4dp，`ui_home_latest_selector` 选中态）：左上日期/`最近7天` 粗 `#333333` + 日历图标；左下 `项目对` `#666666` 上方大数字 32sp `#1478fa`；右侧 `上新项目`/`涉及平台` 标签 + 值 16sp 粗；下接排序头 `ui_home_item_ticker_sort_title` + `recycler_pair_content`
- 文案：`近期上新`、`项目对`、`涉及平台`、`上新项目`、`最近7天`
- 数据需求：Binance `GET /fapi/v1/exchangeInfo` → `symbols[].onboardDate`（永续新上线）；OKX `GET /api/v5/public/instruments?instType=SWAP` → `listTime`。「涉及平台」计数为多所聚合 → 无公开源
- 分类：**B**

## 模块 9：`ui_quote_*` 列表壳

- 布局：`ui_quote_frg_page_frame.xml`、`ui_quote_compat_frg_price_list.xml`（SwipeRefresh + 表头 36dp `ui_ticker_item_price_list_recycler_title` + 1px + `SwipeMenuRecyclerView` + 回顶浮钮 + 自选空态 `ui_ticker_include_optional_empty_view_select_hot`）、`ui_quote_compat_item_price_list_recycler_title.xml`（编辑图标左距 15dp、分类列、`最新价` 排序、右列 120dp 内 涨幅排序 + 横屏图标 左距 8dp、右 padding 15dp）
- 数据需求：`ticker/24hr`（Kanpan 已有）
- 分类：**A**（行情列表通用壳，行布局归行情列表模块）

---

## D 类汇总（不复刻，仅列名）

热门合约 1H 关注度（1.5）；平台爆仓多所占比（2.3）；爆仓人数（2.7）；价格分布/预估清算图 WebView（2.8）；机器人爆仓消息卡（2.9）；币种基本面/资金/安全/团队/持币地址/平台占比环图（3.3，`act_fund*`、`fund_*`、`ui_ticker_frg_brief_detail_fund`、`ui_ticker_frg_share_fund`、`ui_ticker_fund_*`）；大单/主力/跨平台大单/VIP 大单卡/大单预警（4.3）；下单页盘口（4.4）；币址查询（5.3）；挖矿数据（6）；事件日历（7）；
以及前缀内的纯交易/资产/其它：`frg_futures_open_interest` / `frg_bitmex_open_interest` / `frg_bybit_open_interest` / `item_futures_*_interest*` / `part_futures_interest_head|bottom` / `frg_futures_overview*` / `item_futures_coin` / `item_futures_tips` / `act_trade_futures` / `frg_futures_trade_layout` / `part_futures_last_price`（用户持仓、权益、`预估强平价`、`调整杠杆`、`平仓`、`市场有风险，投资需谨慎`）；`act_statistics_*` / `frg_statistics_*` / `item_statistics_*` / `layout_statistics_profit` / `dialog_statistics_unit`（资产统计、授权统计、其他资产录入）；`ui_ticker_frg_brief_detail_statistics` / `ui_ticker_frg_share_statistics`（平台成交占比环图）；`act_futures_asset*` / `activity_total_assets` / `fragment_assets_pie_distribution` / `view_assets_distribution`（合约资产、U本位/币本位）；`act_influence_land_list` / `frg_square_influence` / `ui_influence_page_title`（KOL 排名）；`ui_ace_*` / `ui_home_ace_bottom_sheet`（AI 助理）；`incoming_*`（AI 对话消息卡）；`ui_kline_back_test_result_layout`（回测）；`ui_last_comment_title` / `ui_kline_item_kline_master_ranking_last` / `part_*_last_price`（评论、大师榜、下单页价格）；`carbon_*`（第三方 UI 库）。

---

## 汇总表

| 子功能 | 分类 | 关键布局 | 数据源 | 缺什么 |
|---|---|---|---|---|
| 1.1 首页 BTC 合约概览卡 | B | `ui_home_frg_futures_data`, `ui_home_frg_part_futures_data` | Binance `openInterestHist`(已有) + `globalLongShortAccountRatio` + WS `!forceOrder@arr`；OKX `open-interest`、`rubik long-short-account-ratio`、`liquidation-orders` | 多空比、爆仓累计；「全网」口径无源；市场热度归 D |
| 1.2 持仓量榜 + 7 日持仓 | A | `ui_ticker_frg_futures_interest`, `ui_ticker_item_futures_interest_list_recycler_content` | Binance `/futures/data/openInterestHist`、`/fapi/v1/openInterest`；OKX `rubik open-interest-volume` | 网关线路不能代理 `/futures/data/*`，需 OKX 替代或直连限定 |
| 1.3 资金费率榜 | B | `ui_ticker_frg_futures_fee`, `ui_ticker_item_futures_fund_list_recycler_content` | Binance `premiumIndex`、`fundingRate`；OKX `funding-rate`、`funding-rate-history` | 全部费率接口 |
| 1.4 价差率 / 基差 | B | `ui_ticker_frg_futures_diff_list`, `…_diff_list_recycler_content`, `…_diff_titile` | Binance `premiumIndex`、`/futures/data/basis` | premiumIndex/basis；跨平台基底无源 |
| 1.5 热门合约 1H 关注度 | D | `ui_ticker_frg_futures_hot`, `ui_ticker_item_futures_hot_rank_content` | 无公开数据源 | — |
| 1.6 合约异动 | C | `ui_ticker_frg_change_futures` | 由 Kanpan K 线/ticker 自算 | 行布局与异动类型枚举需真机镜像 |
| 1.7 合约列表（交割/永续） | A | `ui_ticker_frg_futures_fix_list` + 表头 | `exchangeInfo` + `ticker/24hr`(已有) | 交割合约需 exchangeInfo 过滤 |
| 1.8 多空比占比条 | B | `PositionPercentBar`（`ui_home_frg_futures_data`, `ui_ticker_block_detail_list`） | Binance `global/topLongShort*`、`takerlongshortRatio`；OKX `rubik long-short-account-ratio`、`taker-volume` | 全部多空比接口（Binance 侧直连限定） |
| 1.9 合约详情扩展字段 | B | `ui_ticker_include_detail_price_block`, `…_price_extra_block`, `ui_ticker_detail_block_tip_dialog` | `premiumIndex`、`openInterest`、`depth` | FR/预测费率/基差率/委比 |
| 2.1 爆仓总览网格 | B | `ui_ticker_part_futures_liqui_summary`, `ui_ticker_frg_liq_stat_all`, `ui_ticker_item_liq_summary*` | WS `!forceOrder@arr` 自建累计；OKX `liquidation-orders` | 后端持续订阅落库；占全网/集中区间无源 |
| 2.2 7 日爆仓日柱 | B | `ui_ticker_part_liq_day_summary`, `ui_ticker_popup_liq_graph` | 同上按日聚合 | 7 天历史需自建 |
| 2.3 平台爆仓 | D | `ui_ticker_part_liq_plat_summary`, `ui_ticker_item_liq_plat_summary` | 无公开数据源 | — |
| 2.4 大额爆仓流 | B | `ui_ticker_part_liq_recent`, `ui_ticker_item_recent_liq` | WS `!forceOrder@arr`；OKX `liquidation-orders` | forceOrder 流 |
| 2.5 爆仓排行 | B | `ui_ticker_part_liq_rank_list`, `ui_ticker_frg_liq_rank_list_cycle`, `ui_ticker_item_liq_rank`, `ui_ticker_frg_liq_big` | 同 2.1 排序 | 24H/3 日累计 |
| 2.6 项目爆仓 | B | `ui_ticker_frg_liquid_coin`, `ui_ticker_item_liq_coin_body` | 同 2.1 按 symbol 聚合 | 占全网无源 |
| 2.7 爆仓人数 | D | `ui_ticker_frg_liquid_people`, `ui_ticker_popup_liq_number` | 无公开数据源 | — |
| 2.8 价格分布 / 预估清算图 | D | `ui_ticker_frg_liquidation`（WebView） | 无公开数据源 | — |
| 2.9 机器人爆仓卡 | D | `view_page_item_futures`, `item_liq_*`, `incoming_liq_condition_message` | — | — |
| 3.1 资金流向榜（竖/横） | B | `ui_home_act_home_fund`, `ui_home_item_fund_flow(_content)`, `ui_home_land_*fund*` | Binance `aggTrades`/WS `aggTrade` 自累计、`takerlongshortRatio`；OKX `rubik taker-volume` | 主动买卖成交累计；多所「主力」口径无源 |
| 3.2 自选资金列 | B | `ui_optional_item_fund_list_recycler_content` | 同 3.1 | 同 3.1 |
| 3.3 币种基本面/资金页 | D | `act_fund`, `fund_frg_*`, `ui_ticker_fund_*`, `ui_ticker_frg_share_fund` | 无公开数据源 | — |
| 4.1 买卖盘 | B | `frg_ticker_orderbook`, `item_ticker_orderbook`, `act_ticker_ordercount` | Binance `depth` / WS `depth20@100ms`；OKX `market/books` | depth |
| 4.2 成交明细 | B | `frg_ticker_dealamount`, `item_ticker_dealamount` | Binance `aggTrades` / WS `aggTrade`；OKX `market/trades` | aggTrades |
| 4.3 大单/主力/VIP/大单预警 | D | `ui_kline_part_large_order_info`, `frg_indicator_filter_large_order`, `ui_vip_*large_order*`, `ui_alert_dialog_alert_big_order` | 无公开数据源（VIP） | — |
| 4.4 下单页盘口 | D | `part_futures_order_book`, `item_futures_order_book`, `item_portrait_order_book` | — | — |
| 5.1 板块列表 | B | `ui_ticker_frg_blocks_list`, `ui_ticker_item_block`, `ui_ticker_item_blocks_list_recycler_title` | `ticker/24hr`(已有) + 自建板块映射 | 板块-币种映射、总市值；1H 关注度无源 |
| 5.2 板块详情 | B | `ui_ticker_block_detail_list`, `ui_ticker_block_detail_dialog_list_item`, `ui_ticker_block_detail_tag_item` | 同 5.1 | 同 5.1 |
| 5.3 币址查询 | D | `act_block`, `frg_block_query`, `act_block_custom_add` | — | — |
| 6 挖矿数据 | D | `act_miningdata`, `frg_miningdata`, `item_miningdata(_two_row)` | 无公开数据源 | — |
| 7 事件日历 | D | `act_calendar*`, `item_calendar_*`, `ui_news_dialog_calendar_reminder` | — | — |
| 8 近期上新 | B | `ui_home_act_latest_update` | Binance `exchangeInfo.onboardDate`；OKX `instruments.listTime` | 上新列表；「涉及平台」无源 |
| 9 `ui_quote_*` 列表壳 | A | `ui_quote_frg_page_frame`, `ui_quote_compat_frg_price_list`, `ui_quote_compat_item_price_list_recycler_title` | `ticker/24hr`(已有) | — |
| D 类其它（交易持仓/资产统计/合约资产/KOL/AI/回测/评论/carbon） | D | 见「D 类汇总」 | — | — |

计数：A 3、B 17、C 1、D 13（含末行合并项；按子功能行计）。
